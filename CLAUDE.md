# CLAUDE.md · Interplanetary Groups

This file loads every session. It is the standing context and the rules that must hold at all times, including inside any plan that assumes no project context.

**What loads alongside it.** `docs/build-notes.md` is the decision record: the deep reference for the reasoning, the alternatives rejected, and the lineage behind every product decision, with §11 as the per-slice record home (one ADR-style entry per build slice). Consult it during brainstorming and whenever a question is not answered here. Superpowers is in play and owns *process* (TDD, brainstorming, planning, code review, git workflow); do not restate process here.

**The division of labor between the two files, so the overlap does not read as an error worth "fixing".** CLAUDE.md carries the rule in operative form; build-notes carries the reasoning, the rejected alternatives, and the lineage. This is not duplication: CLAUDE.md loads every session, build-notes is consulted, so a rule that must always hold lives here and its story lives there. Where the two disagree, CLAUDE.md is the rule and build-notes explains why the rule exists. Keep this file terse; where it has absorbed a full build-notes rationale, trim to the rule plus a short why, because that is where drift starts.

**How the work is split, and by whom.** One person holds both the product-management role (owning what the product should do) and the engineering role (building it); there is no separate party handing over a framed plan, so this file has to supply the product framing itself rather than assume it arrives from elsewhere. Settle anything that shapes product behavior as a product question first, then build. On pure implementation detail, pick the simplest thing that works and record any notable choice: a line in the PR, and a build-notes §11 entry when it is a lasting decision. §11 is this project's permanent home for "why was it done this way," so a decision made in passing is not done until it is written there.

---

## Where the build is

*Rewritten at each slice boundary. It lives here, not in build-notes, because it churns every slice and must load every session.*

Onboarding is fully built end to end, and now counts its own steps: the founder describes the group in free text (step 1), Haiku extracts structured rhythms, the normalize layer turns that claim into stored shape, a gap-ask conversational loop resolves a missing or ambiguous day or time (never a venue, which never gates), the founder can add the group's meeting spot at the playback step (step 2), deterministic code composes the playback, and confirm creates the group and hands the wizard straight into a third step that shares the new invite link, from which the founder heads into the group home carrying their first scheduled event and their detected timezone. (Amended 23 Aug 2026, pre-deploy-fixes slice: true on the happy path, and the failure branch is deliberately fail-soft. When the first-event reconcile fails, group creation still succeeds and the founder lands on an **empty** group home, on the first screen after creation, for up to an hour, with nothing on screen saying so. Noticed by the audit's completeness critic, not fixed here. ~~`create-group.ts` logs "first-event reconcile failed (cron will catch up)".~~ Corrected 23 Aug 2026, same slice, final review: this slice's own task 4 changed `reconcile.ts` to catch a per-group failure and return a `failed` result instead of throwing, so `create-group.ts`'s catch no longer fires for it and the line that actually prints is `[orbit-reconcile] group failed:`. What the founder sees is unchanged; only the record moved, from a raised error to a `failed` entry in the reconcile result, which is where a future reader should look.) Wizard-header and step details below, joining-arc slice.

**Spark is complete end to end. Orbit listens, gauges, and creates.** Every member message goes to the model after the send settles (never inside it, so the chat input is never blocked). When someone floats an idea Orbit proposes a specific day and time with three one-tap chips and a tally derived from the vote rows, naming who is in ~~and counting down at one away from the bar~~ (amended 17 Aug 2026, event-copy pass: the chat tally no longer counts down, because Orbit's own message right above it already states the bar; the idea card's tally keeps its countdown, because nothing on the card states it). The third yes creates the event in that same tap: the gauge answers carry through as RSVPs with no second tap, the venue is inherited from the group's own rhythm when the activity matches, Orbit announces it naming the day and time, the gauge closes, and the group home carries a second card. Orbit also still runs on an hourly Vercel cron that auto-creates the next recurring occurrence, and a sparked event can no longer suppress it.

Where a sparked event's time comes from, since nothing in the product knew: a stated time wins; am/pm is read from the activity ("beers at 8" is 8pm, "breakfast at 8" is 8am); a genuine coin flip keeps the stated hour, lands pm, and is disclosed once; nothing stated falls to Fri 7pm for evening ideas or Sat 9am for morning ones. All four fallbacks are undated placeholders, deliberately in one module, with override-learning (build-notes §5) as their named successor. **Observed failing in front of a person, 26 Aug 2026, recorded 2 Sept: "we should grab beers next week" was answered with "Beers this Friday?" for a Friday two days later.** Every layer behaved as specified; the cause is that no extraction field can hold a horizon, so "next week" is dropped at the schema before any fallback runs. It is an input with no data home, not a misread, and it is **not** the editing slice's problem despite the two being floated together on 1 Sept. ~~Two candidate fixes, neither chosen and both the owner's to rank: stop asserting "**this** Friday" when no day was named (one string, no bench run), or add a horizon field the date fallback respects.~~ (Corrected same day: **the copy-only fix is withdrawn and there is one real fix, the horizon field.** `buildGaugeMessage` already switches to "on Friday, Aug 28" past a week, so at two days out "this Friday" is an accurate description of the date; the wrongness is entirely in the date. **"Next Friday" is broken too and worse**: a stated weekday is taken at face value with no buffer, so it also lands on the soonest Friday while the member said otherwise. The owner's reading is the spec: on Wed 26 Aug, "next Friday" means Fri 4 Sept. Where people genuinely disagree about "next Friday", reuse the am/pm pattern, pick a reading and disclose it once, rather than asking.) **Built 3 Sept 2026 (next-week-horizon-fix slice): the horizon field is the fix that shipped**, exactly as scoped above. A week runs Monday to Sunday, so "next week" is the week after the one today sits in, chosen over simply adding seven days because that rule is wrong for every weekday Mon-Fri named on a Sat or Sun; "this week" suppresses the same notice buffer that used to push a same-week ask into the following week; Orbit picks a reading and discloses it once rather than asking, reusing its existing am/pm habit rather than breaking the concrete-first guardrail. Verified on a real dev server: "pizza next friday" on a Thursday now proposes a plan nine days out, where before this slice it would have said "this Friday" and proposed the soonest one. **Still not fixed, stated plainly:** a member who was misheard still cannot say "no, I meant the week after" and be understood, because that phrase names no weekday either; recovery still runs entirely through the weekday-shaped day-comment path. Bench numbers, two questions left open for the owner (one bench case that will read permanently red for a behaviorally inert reason, and an unresolved question of whether the prompt change cost any recognition accuracy), and the debt this opens (a Monday-start week and a disclosure string joining the undated-placeholder family that override-learning, build-notes §5, replaces as one decision; and a bench that can grade only the model's claim, never the resolved date) are in build-notes §11, "the next-week horizon field lands." Reasoning in build-notes §11, "next week got answered with this Friday", and its same-day correction.

**An idea now has an ending as well as a beginning.** A gauge still short of three yeses gets exactly one bump around 8pm the evening before its day: a last call naming who is in and what one more yes does, or a gentler surfacing when nobody answered at all, carrying the same chips and the same live tally so it can be answered where it lands. It closes two hours before the proposed start, and only a gauge somebody had said yes to gets a goodbye ("Beers didn't come together this time. Maybe next week."); a gauge nobody answered still scrolls away in silence. (The one exception, added below by the wrong-day retry: a gauge the day alone blocked gets a question instead of the goodbye, or, since the day-comment slice, a straight revival when somebody already named a better day.) A rally floated inside that two-hour window still opens, runs to its start rather than closing early, and names the clock out loud so people know it is for today.

**Navigation is built app-wide, and there are no dead ends left.** `/` is a session-aware front door: a session with a group is sent straight in, a session without one gets the product's pitch and one teal "Start your group." Event detail and group info carry `‹ [group name]` pointing at that group's home (a fixed parent link, never browser history, so it is right for someone arriving from a shared link). The group home's Orbit logo is a real home button, `/create` step 1 has a way out (step 1 only, since later steps already go backwards within the flow), a bad invite token gets a note from Orbit plus a way in, and a wrong id or a render error lands on a branded screen with an exit. Under all of it, `src/components/` holds the product's first shared UI: a header bar that owns the bar's rules and nothing about content, so it can never grow an opinion about the group home's title chevron. The whole app was walked screen by screen to prove the claim. The repo can now test a component, scoped honestly to those shared pieces; it still cannot test the screens, which are server-rendered and talk to the database.

**Change request is built through part two: a time change is a group decision, not one person's word.** A member asking to move an existing event's time, in plain language, in the group chat, now opens a proposal to the whole group instead of moving the plan alone: Orbit posts the ask with chips for everyone ~~and keeps a live tally~~ (struck 17 Aug 2026, event-copy pass: the tally line is deleted with nothing in its place, because its rule is compound and cannot be stated briefly and because naming who wants to move someone else's plan turns a scheduling question into an argument with a scoreboard; the chip's checkmark confirms the vote and Orbit announces a passed change), and moves the plan only when the new time has at least three yeses (or the whole group, for a group smaller than three) and beats the number of people still in on the old time, counting a yes from someone already in as them switching sides so the bar stays reachable even when a small group is unanimous. The yes that clears the bar moves the plan in that same tap: every RSVP resets because a yes for the old time isn't a yes for the new one, the asker's own message and every other yes carry through as an IN RSVP on the moved plan, and a keep tap gets no RSVP row since choosing between two times isn't an attendance answer, so a keep voter returns to pending and the card asks them again. A bare hour still inherits the plan's own part of day, disclosed once, and "put it back" is still just another change request, now moving through the same group vote as any other time change. **A vote that stalls now ends (18 Aug 2026):** the hourly sweep closes it, records LAPSED, and posts one soft line naming the time the plan is staying at, while a vote made moot because the plan moved some other way records SUPERSEDED and says nothing; a member who has voted also sees one quiet line on the event screen confirming it counted, rendered per viewer and never posted to chat. Orbit's ask itself now ends "Move it?" rather than "Works for you?", so the question matches the chips under it. Venue, day, and rhythm changes still get an honest decline naming what Orbit can't do yet.

**Orbit now reads the conversation before deciding what a message means, and a direct ask is never met with silence.** Detection reads the last twenty messages in the group feed, including Orbit's own, each with a timestamp and a now-anchor, so a bare follow-up lands on the plan the group was just discussing, a correction is understood as a correction, and a newer proposal on the same plan or from the same person supersedes the one before it automatically. A request Orbit can't fully resolve now gets a question instead of quiet: which plan (or an honest "I don't see any plans on the calendar right now" when there are none), what time, or "it's already at that time" when the ask matches the current one; a probable-but-ambiguous reading still gets the confirm-or-leave-it chip, and confirming now opens the group proposal instead of moving the plan outright. Reasoning, the consensus arithmetic, and the walkthrough evidence in build-notes §11.

**Recognition no longer eats a direct ask before the ladder sees it, and there is now a bench that can prove it.** The never-silent rule had been written into the reply ladder but not into the recognition step upstream of it, so a bare "can we move it?" was classified as not-a-request and died in silence. Three fixes closed it: the intent prompt no longer lists a question asking for a change alongside questions that only want information, its tiebreak now splits "unsure anyone is asking anything" (still stay quiet) from "unsure only what they meant" (say so and let the ladder ask), and a guard in the claim-to-fact boundary that threw away any change request naming no field is gone. `npm run eval:detect` runs the recognition bench: thirty-three cases as of the day-comment slice, each a short setting plus one message plus the outcome a member should see, graded through the real prompt, the real normalize layer, and the real ladder, with no database. It is deliberately not part of the test suite, because it costs money and hits the network. Per-slice baselines and after-numbers are in build-notes §11; the current full-bench reading is 80/80 runs on the sixteen cases Orbit must recognize, 65/65 on the thirteen it must stay quiet about, and 10/20 on the four deliberately ambiguous ones, where the same two cases have failed since the day they were written. (Scoped 23 Aug 2026, pre-deploy-fixes slice: the numbers are real and they measure less than the sentence around them implies. The audit's F-3-20 found no bench case exists for the product's core spark behaviour, F-3-2 found four day-comment cases stopping short of the deciding code, and F-3-27 found the flagship regression case rebuilding its failure with words Orbit no longer says. Fixing the bench is its own slice; the benches were not re-run here because they cost money and hit the network.)

**An idea the day blocked is now asked about, then guessed at once.** A gauge that closes short but would have cleared the bar (in-votes plus can't-that-day votes reaching three, at least one of them a can't-that-day) no longer gets the goodbye. It gets a question instead, in one message: "Beers didn't happen for Monday, but three of you want it. What day works better?" (amended 10 Aug 2026, day-comment slice: the ask is now only the path when nobody named a day while the gauge was live; a remembered day skips it and revives straight away, described below.) A member naming a day gets a fresh gauge with the namer seeded and the original's time carried onto it (amended 5 Aug 2026, answer-seam slice, which is where that path is now described). Nobody answering earns exactly one guess the next evening around 8pm group-local, the same weekday one week after the day that failed, posted as a real gauge with chips and a live tally and the original's time copied onto it, so "beers at 8" is still 8pm a week later. That guess is Orbit's own move and never earns a second cycle however it dies; only a human resets the clock. Two premise corrections worth keeping, because the older framing of this slice got both wrong: the third chip already held half the signal (Orbit knew *that* the day failed and for how many, only not *which* day would work), and the "a gauge whose day has passed is dead" rule was never bent, because every revival is a genuinely new gauge.

**The conversational half now works too: Orbit hears the reply to its own question.** A member answering "Saturday?" or "saturday works for me" while Orbit's day question is open gets a fresh gauge for that same activity, on the day they named, at the hour the group already agreed on, with them counted in. The model gained exactly one judgement, whether this reply answers Orbit's open question and which day it names, and lost everything else: the activity, the time, and whether a question is open at all are read from stored rows. The window is derived rather than scheduled, opening when the ask posts and shutting the moment any same-activity gauge exists (a member's answer or Orbit's own guess), so the answer window and the guess suppression can never disagree; an answer claim arriving with no open ask is discarded whatever the model says, and inside the window the answer reading beats the change reading. The two bench cases went from 0/5 and 0/5 to 5/5 and 5/5, the look-alike Orbit must ignore held at 5/5, the messages Orbit must not answer held at 50/50, and no prompt tuning rounds were needed. Numbers, the browser walkthrough, and reasoning in build-notes §11.

**Unanswered ideas are cards now, and the pending strip is gone.** The strip, its panel, and its wash retired 12 Aug 2026. An idea the group is voting on appears as its own card in the top card region, mingled with confirmed plans in pure date order (a same-instant tie goes to the confirmed plan), five cards combined at most; anything past five is carried by chat, as it always was. An idea card is subordinate by structure and never by hue: a flat shell, a title that is the activity plus a question mark ("Beers?"), the proposed day and time followed by a fixed "Place TBD", and the same three chips recording the same vote as chat, ~~in the same tally voice~~ (amended 14 Aug 2026, card-region-height slice: the card's tally took a counts form of its own, ~~"2 in · one more to go"~~ (amended 14 Aug 2026, PR #67 QA fix: "2 in · one more makes it happen", matching Orbit's own countdown phrase; the different-day clause was dropped from the card entirely, since "2 in · 1 for another day · one more makes it happen" ran past the card's 298px ceiling, and the different-day count stays visible in chat), while Orbit's spoken tally in chat kept its named form, ~~"Sam & Jordan are in so far · one more makes it happen"~~ (amended 17 Aug 2026, event-copy pass: "Sam & Jordan are in so far", the countdown deleted); two tally voices now, by design, not one). ~~A card carries a short top-right label naming what it still needs~~ (amended 14 Aug 2026, card-region-height slice: the label is no longer top-right on either card; it now shares the counts line on a confirmed card and the title's row on an idea card, dropping to its own line only when that shared row runs out of room: a long activity name on an idea card, a large group's counts string on a confirmed card), and none at all when it needs nothing: teal when the need is the viewer's own (NEEDS YOUR RSVP, NEEDS YOUR VOTE), grey when it is other people's (NEEDS OTHER VOTES). ~~An open time-change vote shows on its own event's card as one quiet line ("Time change proposed · Move to 8pm?") that opens that event's screen, where the vote itself sits just above "Add to calendar" because it changes the very time that button would save.~~ (struck 14 Aug 2026, card-region-height slice: the confirmed card no longer points at the vote at all. It is raised, announced, chipped and tallied in the group chat like any other time-change ask, using the same chips that already lived there, and it still renders on the event's own detail screen, one tap away rather than named on the card, ~~just above "Add to calendar"~~ (since 17 Aug 2026, just below it: the calendar-placement micro-PR put the button directly under the details card, because beneath the vote it read as saving the proposed time).) Cards stretch to the region's height with the answer row anchored at the bottom, so leftover space is breathing room inside a card rather than dead grey below it. A viewer's decline drops that idea card for that viewer alone. The "you're caught up" note retired with the panel it lived in, deliberately, with nothing in its place. Reasoning, the five named departures from the design boards, and the walkthrough evidence in build-notes §11.

**A day named while a gauge is live is now heard, and it shapes what happens if the idea fails.** A member replying "Sunday works better" instead of tapping a chip gets counted: the vote records as can't-that-day (an explicit "I'm in" is never overridden by an inference), the gauge remembers the named day and who named it, and Orbit replies once, noting the day without promising anything it cannot guarantee, because whether Sunday happens depends on a bar the commenter cannot see. If that gauge later closes blocked by its day and would have cleared the bar, Orbit skips asking the group what day works, since somebody already answered, and opens the named day's gauge immediately: the namer counted in, the original's time carried over, full rights as a member-named gauge. Orbit's own week-later guess earns that same revival, because a person naming a day is a human resetting the clock. Latest comment wins, one reply each, and the remembered day shows up nowhere but that reply. Reasoning, the bench numbers, and the walkthrough evidence in build-notes §11.

**The group has a real reference page now, not a stub.** `/groups/[id]/info` answers who is in the group, when it meets and where, and how to bring someone in, and it carries the group's only self-service actions. Any session sees the identity block (emblem, name, member count), the WHO list founder-first-then-join-order, and every stored rhythm with its venue when it has one. A member or the founder additionally sees the real invite link with a share button (native share sheet or clipboard-copy fallback); a member alone also sees a warmly-confirmed "Leave group" button, which never renders for the founder; the founder alone gets two quiet text links, "Manage members" (per-row remove, founder's own row exempt) and "Reset link" (rotates the token, killing the old link everywhere). Departures, leaving or removal, are silent in the feed, and ~~every count and tally already reads current members only, so removal self-heals with no extra write~~ (struck 23 Aug 2026, pre-deploy-fixes slice: the audit's finding 10 disproves the general claim. Removal deletes the membership row and nothing else, so a removed member's old RSVPs and gauge votes survive and start counting again the moment they rejoin through the same link, which is the documented way back in. The self-healing claim was verified for `src/lib/proposals/promote.ts` alone. Queued as audit finding 10, not fixed here). Everything else about the group still changes only by telling Orbit in chat. Built 10-11 Aug 2026; reasoning, decisions, and walkthrough evidence in build-notes §11.

**The joining arc is built: the founder hands out the link, and the group sees who it brought in.** Confirming onboarding step 2 no longer drops the founder straight into the group; it advances the same wizard to a third step carrying the group's name, the real invite link in a pill, a teal "Share invite link" button (native share sheet or clipboard-copy fallback, the component lifted from the group-info page and shared by both), and an outlined "Take me to my group" that lets them in when ready. The first time a person joins through that link, the group feed gains a quiet centered "Jesse joined" line, no bubble, no avatar, written the moment the membership itself is written so the two can never exist apart; a re-tap of an already-used link joins nobody and announces nothing. Orbit does not read these lines: they are excluded from the twenty-message window it uses to interpret requests, so a join can never be mistaken for a member's message. Built 11 Aug 2026; reasoning, decisions, and debt in build-notes §11.

**The plan can leave the app now: "Add to calendar" is real.** An event's detail screen carries a full-width teal "Add to calendar" pill below the details card; tapping it hands the member's own calendar the plan's title, day and time, meeting spot (with the stored street address, its first surface anywhere in the product), and a line back to the event page. The file is built fresh at each tap from the stored plan, so a tap after a group time-change carries the moved time, and it carries what a calendar app needs to see the second copy as a newer version of the same plan rather than a duplicate (no specific calendar app's behavior was verified). The honest limitation, unchanged from build-notes §6: an entry already saved does not update itself when the group later moves the plan, so the feed announcement stays the correction channel until the post-MVP subscribable feed exists. A member's own calendar showing the event in their local time is correct, not a break in the everything-renders-in-group-time rule; why, with the rest of the reasoning, decisions, and debt, in build-notes §11. Built 11 Aug 2026.

**A group is invite-only now, and Orbit says why it cannot think.** Someone holding a group's URL who is not in that group meets one note from Orbit telling them to ask a member for the invite link, and learns nothing else: not the name, not the member count, not a word of the chat. Same on the event page, the group info page, and the calendar file (which carries the venue's street address). Every write refuses a non-member on the server too, because a removed member's stale tab still holds live buttons: posting, RSVPs, gauge votes, group time-change votes, answering Orbit's question, and Orbit reading a message at all. ~~Promotion now counts current members only, like every other tally.~~ (amended 23 Aug 2026, pre-deploy-fixes slice, final review: promotion itself does count current members only, verified in `src/lib/proposals/promote.ts`, and that part stands. The "like every other tally" half is the same general claim line 41 struck six lines above, so it goes here too rather than quietly putting the belief back: a removed member's old RSVPs and gauge votes survive removal and count again the moment they rejoin. Audit finding 10, not fixed here.) Separately, a failed model call is classified honestly: out of credits, or the service having trouble, never one blamed for the other. Onboarding keeps the founder's text and says which it was; in chat the message still posts and only the sender sees a quiet line that Orbit might miss ideas. Nothing about it is stored.

**The product has its real face now: polish slice one landed the foundations and the group home.** The app runs on the design system's actual palette (`--surface-base`, `--surface-raised`, `--hairline`, `--action`, `--lime` and the rest), dark is the genuine default rather than a media-query branch, Geist is applied everywhere from the body down, and Orbit's letter-"O" placeholder is gone: one shared `OrbitMark` component draws the real mascot (lime planet, moon on an orbit path) at all seven places Orbit appears. The group home is dressed to the approved handoff: the header carries a heading-weight name ~~plus the designed "N members · group info & invite link" subline~~ (struck 17 Aug 2026, header-subline micro-PR, which also deleted the carousel's dot row for another 17px, 33.5px of chat back between them: the subline is deleted, a measured 16.5px of chat back (the header goes 73.5px to 57px; the earlier ~24px was an estimate), the chevron beside the name carrying the "this opens group info" signal alone; accepted cost, the invite link's only advertisement on this screen goes with it, and onboarding step 3 is now the only place it is put in front of anyone unprompted, the group info page still carrying it with a share button one tap behind the title), the carousel has real peek geometry ~~and a dot that tracks the snapped card~~ (struck 17 Aug 2026, header-subline micro-PR: the dot row is deleted, a measured 17px straight to the chat, because the peeking next card already says there is more; the rail kept its swipe and lost all of its state), ~~the pending strip reads as its own full-bleed element with the owner's 7% teal wash~~ (struck 12 Aug 2026: the strip and its wash retired with the card-state-grammar slice), the feed marks its days in the group's timezone, the three chat voices match the boards, the event card carries its finished shell with counts on their own steady line, and the composer is grounded by a scrim with a send button that goes teal the moment there is something to send.

**The screen has an edge now, and an idea has a floor.** The group home's card region and its chat feed used to share one background and no boundary, so the feed appeared to slide under the cards, and the idea card was worst hit: its fill was `--surface-base`, the exact value of the page and the chat behind it, which is to say no fill at all. Round 7 of the design system answered it two ways and the owner picked direction A: the idea card moved onto a new fourth surface, `--surface-low` (#1f222c), sitting one step above the page and one below a confirmed card, and the boundary became its own small piece, `FeedSeam`, carrying a full-bleed hairline where the feed begins plus an 18px scrim over the feed's top edge so a message darkens as it travels up instead of vanishing at an invisible line. The card region's empty-state box, never drawn by anyone before, came out of the page into `CardRegionEmpty` and is now the quietest rung of the ladder: no fill, a dashed hairline, and copy that tells a member what to do ("Nothing planned yet, float an idea in chat") rather than promising Orbit will handle it. The honest limit, measured rather than eyeballed: the idea card sits 1.12:1 from a confirmed card and 1.14:1 from the page, because the whole palette spans only 1.27:1, so fill is the weakest of the five signals separating a maybe from a plan, behind the controls, the shadow, the title weight, and the need label. Reasoning, the rejected direction B, and the walkthrough evidence in build-notes §11.

**The card region has a height budget now, and the chat got the screen back (card-region-height slice, 14 Aug 2026).** A real-phone pass on PR #66 showed the chat squeezed into a strip: on the owner's phone (iPhone 13 Pro in Chrome, which gives the page 661 CSS pixels, not the 844 the device advertises, because Chrome keeps both bars and an inner scroll region never collapses them) the pinned region took **315px, 47.7% of the screen**, and the feed got 208px. It now takes **224.8px, 34%**, and the feed gets **289.7px, 43.8%**. Measured, not estimated. The QA note's diagnosis was wrong and measuring is how we know: the tallest card was the **idea card** at 272px, not the confirmed one, and 169px of it was a chip row wrapping to two rows plus a tally wrapping to two lines, both text-length problems rather than layout ones. Five changes got there: the gauge chips dropped their emoji and kept every word; the card's tally took a counts form (~~"2 in · one more to go"~~, amended 14 Aug 2026, PR #67 QA fix: "2 in · one more makes it happen", with the different-day clause dropped from the card entirely so the longer phrase still fits on one line) while Orbit's spoken tally in chat kept its named form (and, since 17 Aug 2026, dropped its countdown), which is a second tally voice by design; both cards' need labels left their own rows, the confirmed card's joining the counts line and the idea card's joining the title, each row wrapping rather than clipping because the card root hides overflow; the RSVP pair rose to a 44px tap target, free because the idea card sets the floor; and an open time change left the confirmed card entirely. Equal-height cards stay, by the owner's call, and background colour was not a lever. Two design-round approaches were declined in favour of the owner's simpler answer; reasoning, the brief's own arithmetic error, and the measurements in build-notes §11.

**Queued out of that slice's phone pass, none of it blocking:** ~~the header's "N members · group info & invite link" subline goes, worth another ~24px of chat (its own micro-PR)~~ **done 17 Aug 2026**; ~~the time-change tally line is **deleted rather than reworded** and its chips become "Move to 8pm" / "Keep 7pm"~~ **done 17 Aug 2026** (event-copy pass, which also shortened Orbit's spark message and dropped the chat tally's countdown); ~~"Add to calendar" moves to the top of the event screen, since sitting below the time-change block it read as saving the proposed time~~ **done 17 Aug 2026** (calendar-placement micro-PR: the button now sits directly under the details card, above the vote); and a detail page for a pending idea is queued post-MVP, recorded as a candidate answer to the idea-versus-plan distinctness question because "one of these opens and one does not" is already a structural difference. ~~**The next slice is "the time change gets an ending"**, and this slice made it load-bearing rather than tidy-up: with the notice off the card and the tally gone, a stalled proposal is now invisible, and `ChangeProposal` has no expiry, no close and no bump.~~ **Done 18 Aug 2026**, in the shape the owner settled: a close, no bump.

**The event screen and the chat lost four pieces of copy that were saying the same thing twice (event-copy pass, 17 Aug 2026).** The time-change vote's tally line is deleted outright, with nothing in its place: its rule is compound (three yeses AND more than the people still in on the old time, or the whole group when smaller) so no brief line states it honestly, and naming who wants to move someone else's plan turns a scheduling question into an argument with a scoreboard; the chip's own checkmark confirms a vote landed, and Orbit still announces a passed change while every RSVP resets. Its chips now read "Move to 8pm" / "Keep 7pm", because "8pm works" is an availability answer and this vote is a preference between two times. The gauge's chat tally drops its countdown ("Sam & Jordan are in so far"), on a rule worth carrying forward: **each surface states the bar exactly once**, and Orbit's message directly above already does; the idea card keeps its countdown because nothing on the card says the bar otherwise. And Orbit's spark message shortens to "Love it. Beers this Tuesday? If three are in, I'll set it up.", two rendered rows against three, since "Anyone in for X?" and "If three are in" were asking the same question twice. Reasoning and the bench numbers in build-notes §11.

**Registered 18 Aug 2026, not this slice's to fix: the recognition bench regressed on main.** `bare-ask-no-plans`, a must-recognize case where a member asks "can we move it?" with nothing on the calendar, scored 5/5 twice on 17 Aug and now scores 7/10 on main and 3/10 on the time-change branch (the spread between them is noise at n=10; the failure is on both). The changed copy provably cannot reach that case's prompt, so this reads as model-side drift on a day the API was returning 529s. It matters because the failing behavior is a direct ask met with silence, the exact bug the 29 July tune-up fixed. ~~Re-run `npm run eval:detect -- 10 bare-ask` before deciding anything; a settled reading needs a calmer day than 18 Aug.~~ **Settled 19 Aug 2026: it reproduces, and the owner accepted it.** The calm-day re-run scored 5/10 on main with the API behaving normally, so it is drift rather than weather (10/10 on 17 Aug against 12/20 on main since; at the ~60% rate now measured, a 10/10 run would happen about 0.6% of the time). The owner's call, made on the member experience rather than the number: about half the time a bare "can we move it?" into an empty calendar gets silence instead of the honest "I don't see any plans" answer, and silence is acceptable where invention would not be. **Invention is structurally unreachable on this path** and that was verified, not assumed: `change-plan.ts` counts real stored events and returns a fixed `NO_PLANS_REPLY` constant, so the model supplies no prose here at all. The case moved to a new fourth bench bucket, `accepted`: still scored, still printed, no bar, so `must-recognize` reads 75/75 and a red bar means something again. Revisit if the rate slides further or the trigger stops being rare (it needs an empty calendar, which is essentially a brand-new group). Reasoning in build-notes, 19 Aug 2026.

**Polish slice two landed the onboarding wizard (18-19 Aug 2026).** The wizard now speaks one bubble grammar: `StepGapAsk` and `Step2Playback` moved onto the shared `OrbitBubble`, closing slice one's recorded debt. Step 1 carries the design's two-triangle tail (the product's only tailed bubble, where a hairline triangle behind a surface triangle makes the tail's slants read as the bubble's own border), the designed field shapes, and a pill Continue button. The playback rows left the Orbit bubble for their own `PlaybackCard`, shared by the playback and gap-ask steps, with the dashed lime gap marker, its lime clock glyph, and the teal confirm footer band. The gap-ask input is a 26px pill; its send button and the group chat's are now one shared `SendCircleButton` at the design's 40px. Step 3 has its 46px teal share pill and a deliberately non-teal proceed button, and the shared pill updates the group info page too, by the owner's call. The header's Orbit mark sits in its 44px slot uncropped, the wordmark at weight 800. `OrbitPause` was never drawn by anyone, so by the owner's ruling its shape stands and only its spacing and voice were tuned. The wizard's error red is now the `--danger` token.

**Two things slice two deliberately did not settle, both the owner's.** The design's character counter reads "0/500", which imposes a 500-character cap on the founder's description where none exists today; that is product behavior, it was surfaced as a question, and the task was split rather than guessed at, so the counter is unbuilt and the field stays uncapped. And the group name renders as a proper title bar on step 3's card and in the design's own step 2 board, but as a wrapping "GROUP NAME" key/value row on step 2 and the gap-ask; our step 3 and the design agree against our step 2, and converting it is structural because that name is an editable input. Both queued, neither decided.

**One rule this slice proved twice, worth reading before touching the handoff CSS: map its tokens by value, never by name.** `--ink-faint` looks like `--text-faint` and is actually `--text-secondary` (#A7AAB6); mapping it by name dimmed every schedule-row key, and the same error was later repeated in a task brief and caught only because the implementer followed the source over the brief. Related and equally load-bearing: a later override pass in `walkthrough.css` supersedes an earlier definition, so the LAST definition of a selector is the correct one. And the design's fixed geometries were drawn against short example data: its 62px key column assumed "CLIMBS" while real extraction yields the founder's own word, so a fixed 62px plus break-word rendered "CLIMBING" as "CLIMBI / NG". The column now sizes to content with a 62px floor and a 60% ceiling; the accepted cost is a ragged value column, with a card-level grid queued as the fix that would give both.

**Polish slice three landed the five remaining screens (21 Aug 2026), and the visual-polish pass is complete.** The front door is the designed screen now: the 104px Orbit mark holding the top, the pitch and one teal pill pinned to the bottom so the space between absorbs longer text, and the "Already invited? Open the link you were sent" note that is deliberately not a second button. The join screen stopped being a bare form: a "You're invited" eyebrow, Orbit introducing itself by the group's name, and a card carrying the group name, member count and stored rhythms, so someone holding an invite link sees what they are joining before they join it (intended, by the owner's call; the invite link is the credential and this does not loosen the members-only wall). Event detail moved its title inside the card, replaced key/value metadata with icon rows, moved the RSVP pair into a card footer band, and put the roster's three groups in one card divided by hairlines with names wrapping horizontally, which is what lets a nine-person roster fit a phone. Roster avatars lost their per-name hue, closing registered debt: colour that carried no meaning had no business on the one screen where status must never be read from hue. Group info was a values port. The three dead-end screens kept their undrawn shape and lost their outer eyebrow.

**Two copy changes, both authorized, one of them a correctness fix.** The join screen's old reassurance line promised email sign-in the product does not have; it now reads "No app to download, no password. You'll land right in the group." And the members-only wall and bad-invite screen dropped the outer eyebrow that restated their own note's opening clause.

**One rule this slice added, and one it corrected.** Added: "nobody drew this screen" and "nobody drew this element" are different claims. The dead-end screens are undrawn, but the note *inside* `OrbitNoteScreen` turned out to be drawn as `.ed-slip`, so it got a real port while the screen around it was left alone. Corrected: a passing test is only evidence if it could have failed, and this slice is the first time that was enforced by actually breaking the code to check. The eyebrow guard passed happily until a reviewer restored the deleted prop and found the test could never have seen it.

**Registered as debt, not fixed:** at enlarged device text a long single-word label still breaks mid-word once it hits the key column's 60% ceiling, on group info, the join card and `PlaybackCard`. This is NOT slice two's accepted raggedness tradeoff (a made-up word at default size); it is a real word at accessibility text sizes, the same class as the CLIMBI/NG bug. The queued card-level grid (`minmax(58px, max-content) 1fr`) is the fix, and it belongs at the pattern. **Shipped knowingly:** a declined member's name renders at 4.4992:1, a hair under WCAG AA; it is the design system's own token pairing, status never depends on it, and the ladder is hue-free. **Queued:** on group info, `LeaveGroupButton` is a pill because it was drawn while `ManageMembers` and `ResetInviteLink` stay rectangular because they were not.

**Still unported deliberately:** `ChatInput.tsx` and `choice.tsx` carry a hardcoded `#f87171` where `--danger` exists, left as out of lane.

**The product is live at https://interplanetarygroups.com** (custom domain since 26 Aug 2026; originally at https://interplanetary-groups.vercel.app, 25 Aug 2026, which still serves). All sixteen pre-deploy checklist items are done, and the checklist is closed with a dated note at its head in build-notes §11. On Vercel Pro (Hobby refuses this repo's hourly cron outright, rather than degrading it), functions in `cle1` beside the Ohio database, ~~on the free `*.vercel.app` host with a custom domain still deferred~~ (amended 26 Aug 2026: the domain is **interplanetarygroups.com**, $12/year, bought through Vercel and serving the app, with `www` 308-redirecting to it and the `*.vercel.app` address still serving as well. **It was forced rather than chosen**, which is why it landed outside any slice: Supabase's built-in email sender reaches only members of the Supabase organisation, and every real sending service requires a domain whose DNS you control, so the email sign-in slice could not deliver a login code to anyone without one. Login mail sends from `account.interplanetarygroups.com` through Resend, on a subdomain rather than the root so a future digest gets its own sending reputation. **Three addresses now serve this product, and a session belongs to exactly one of them**, so a person signed in on one is a stranger on the others; that is registered in build-notes §8 as its own finding and it is the thing to know before adding a fourth.) ~~The 14 migrations applied to production in one run, which is the first actual proof the migration history builds a working database.~~ (Struck 24 Aug 2026, after the owner wiped his production test data and we looked at what was underneath: the production project was **not created fresh for the deploy**. It is the original June development database, repurposed, and the one-shot experiment's database became dev-test. Rows dated 22 to 26 June were still sitting in it, two months before the product went live. So the migrations did not run against an empty database and that sentence claims a proof that never happened. **Settled the same day by running that query.** The fourteen applied in two sittings, not one: the first four on 19 to 26 June, when this was still the development database, and the remaining ten together on 24 August at 23:34 UTC, in about ten seconds, on top of them. So the sentence above is simply false and the deploy proved less than it claimed. What the timestamps do establish, and it is most of what matters: migrations one to four ran against a genuinely empty database in June, and five to fourteen ran as one clean sequential batch on top of them in August, so **every migration has now been applied in order, on top of its predecessors**, just never in a single run. The residual risk is therefore narrow rather than open-ended: whether all fourteen apply in one command to a brand new empty database. Prisma applies them as ordered DDL and none of them backfills or depends on rows (established by the task 9 migration read, `docs/audits/findings/2026-08-23-migration-read.md`), so the expectation is that it works. It is still an expectation. ~~**The preview environment queued in §8 requires a new Supabase project, which is the free and deliberate chance to turn it into a fact; do it there rather than waiting for a disaster to ask the question.**~~ **Answered 26 Aug 2026, and the answer removes the occasion rather than taking it: the owner decided there will be no preview environment, no staging URL and no third database.** Four reasons, his: the only users are him and one friend, so the audience a staging URL protects does not exist; a group either would actually keep belongs on production rather than a copy that gets thrown away; he already runs the manual QA script on his own phone against every PR, which is the gate staging would have provided; and the inbox test the email slice needed was reachable from his laptop against dev-test. **So the never-verified question stands with no scheduled occasion to answer it: whether all fourteen migrations apply in one command to a brand new empty database.** Whoever creates the next Supabase project for any reason should run it there and record the result.) Both model calls are proven in production, extraction through the wizard and detection through a floated idea Orbit answered with chips and a tally. Two things the day established that a future session must not re-derive: **the Data API on the production Supabase project is off, and that is what makes "no RLS policies" safe**, so switching it on later opens every table to anyone holding the browser-shipped key, in that same moment, with no error; and **`connection_limit` and `pgbouncer=true` are both inert on this stack**, the pool ceiling being `max` in `src/lib/prisma.ts`. Reasoning, the seven decisions, and eight findings in build-notes §11.

**Queued out of the deploy, none of it blocking, all of it in build-notes §11 with reasoning. The owner ranked two of these himself on 25 Aug 2026, and that ranking is his rather than a build inference: the message latency and the missing way into a second group both sit near the top, straight after email sign-in.** `create-group.ts:73` and `join-group.ts:43` throw away Supabase's error before returning their friendly message, which is why the product's most expensive failure is expensive; a one-line log is the fix. The two `NEXT_PUBLIC_` Supabase variables never reach a browser (only `server.ts` and `proxy-session.ts` read them), so the prefix buys nothing and costs a full rebuild every time a Supabase setting changes. ~~**Audit finding F-6-2 is confirmed on a real phone rather than theoretical**: a six-message group takes three to four seconds to send, because the app reloads the whole chat history and re-renders twice per send, so the chat gets slower the more the group talks; recommend ranking it above where the audit left it.~~ (Struck 31 Aug 2026, message-send-latency slice one. **The symptom was real and the stated cause was wrong**, which is why this is struck rather than ticked. Measured: at six messages the history query is 12% of a render, and growing a group from 6 to 500 messages costs 17% more render time, so it cannot explain a slow send on a six-message group. What the three to four seconds actually was: the member's own message reached the screen in 6-19ms and then sat at 65% opacity, reading as *not sent yet*, until 5-6.5 seconds later, released only when Orbit's model call finished. F-6-2 was marked *confidence: certain* and confirmed on a phone, and it was still wrong, because the phone confirmed the symptom while the mechanism was derived by reading. The unbounded query remains real debt and is slice two's. Full measurement, the three wrong explanations, and the two corrections to F-6-2's other claims in build-notes §11.) ~~There is no way into a second group from inside the app (`/create` typed directly is the only route, and a session holding two groups reaches only the most recent), which is far smaller than the multi-group home that is deliberately out of scope and should be queued apart from it so it does not wait on something unplanned.~~ (Struck 1 Sept 2026, second-group-entry-point slice: there is a way in now. Tapping the Orbit mark from inside any group opens `/groups`, a list of the groups you are in, and that screen carries "Start a new group". Queuing this apart from the multi-group home was the right call and is worth keeping as evidence for the next time the two get bundled: it landed in a day, and the multi-group home is still out of scope. What it landed as is in "Where the build is" above; reasoning in build-notes §11.) Four dependency install scripts are blocked by npm's `allow-scripts` gate on Vercel; the build is green and nothing is known to be broken, and it is unexplained rather than dismissed. And the Supabase project is free-tier, which pauses after about a week idle: the hourly cron prevents that by accident, so if the cron ever stops the site dies a week later for a reason nobody would connect to it.

~~**Queued 25 Aug 2026 at the owner's request, and it is a rewrite rather than a tidy-up: `README.md` is roughly six weeks behind the build.**~~ **Done 31 Aug 2026, readme-rewrite slice; entry kept for the survey it records and for the one item it got wrong.** Every stale line named below was confirmed still stale and fixed, the wrong session-loss line is corrected and split by whether the member ever attached an address, and everything in the missing list is now in "Working end to end" along with email sign-in and the digest. **One thing the survey shows rather than says, worth keeping visible:** the test figure it quotes as the true one, "955 across 97", was accurate on 25 Aug and was 1374 across 126 by the time the rewrite ran. The number was taken by running the suite rather than by copying the record, which is the only reason the rewrite did not inherit a stale figure while fixing one. A count inside a queue note is a fact about a day, and a queue note outlives its day. Original survey follows. Surveyed on the day, not guessed at. Wrong or stale: the "no hosted demo yet, a Vercel link will go here" banner (there is one); "a daily scheduled job" and the Hosting row's "daily cron" (hourly since 18 Aug); "Next up: the one-bump resurface" (shipped 4 Aug); "517 tests across 38 files" (955 across 97); and "a visual polish pass is pending, and some scaffolding defaults from project creation are still in place" (three polish slices landed, scaffolding closed 11 Aug). Missing entirely from "Working end to end": idea cards and the card-state grammar, the gauge endgame with the wrong-day retry, answer seam and day comments, the group info page with its self-service actions, the joining arc and join announcements, add-to-calendar, the members-only wall, and the brand assets and link preview. **One line is not merely stale but wrong in a way that understates a real bug:** the known-gap claim that a member who loses their session "sees the invite-only screen until they tap the invite link again." They do not. They rejoin as a *second person* and every count quietly stops being true, which is the whole argument for the email slice, and slice A struck that same claim from this file on 23 Aug without the README being touched. **Worth ranking on audience rather than tidiness:** this repo's stated purpose is to be read by engineers evaluating product-and-engineering fluency, and the README is the first thing they open.

**`ai-build-process` exists (31 Aug 2026).** The public, curated subset of the private `~/.claude` backup, at `github.com/jacobrios/ai-build-process`, working copy at `~/code/ai-build-process`. This README already links to it.

**Trigger: when THIS repo goes public, two things must happen.** First, `ai-build-process`'s README says "its repo is private for now" and links to `interplanetarygroups.com` rather than here; both stop being right in that moment, and it is recorded on this side because nothing in that repo can see this repo's visibility. Second, re-check this README's "Deferred on purpose, and queued" list first, since the second-group entry point may land before anyone reads it. Reasoning in build-notes.

~~**Reminder the owner asked for, by name: after the email sign-in slice, revisit web push versus a thin native notification shell.**~~ **Answered 1 Sept 2026: no rung, on cost against evidence, not principle.** The reason for wanting push turned out to be per-message chat, which is a better case than the one queued here and which broke build-notes §6's "never per-message notifications" rule; that rule is amended rather than left to argue against him later. **Trigger: digest "you missed" counts going regularly non-trivial.** Reasoning, the noise budget, and the itemised cost in build-notes §11, "the push question, answered".

~~**The product is ready to be deployed, though it has not been (pre-deploy fixes, 23 Aug 2026).**~~ (Struck 25 Aug 2026: it has been. Entry kept for what the slice decided.) The app wears its own face in a browser tab and on a home screen instead of the starter kit's, and a texted invite link arrives as a card naming the group rather than as a bare URL, on one static image the whole product shares (the accepted cost: unfurling services read a group's name with nobody tapping). Search engines get the front door and nothing else, so a group's page can never turn up in a search result. The invite token is generated the same cryptographic way the reset button already used, so one link cannot be guessed from another. Orbit's hourly job survives one bad group, where a single group's data used to cost every later group its recurring plan and both sibling sweeps for that hour. And the framework was updated for twelve security advisories before the URL goes public. Reasoning, the seven decisions, and the metadata bug no test could have caught in build-notes §11.

**Email sign-in is built, and a member can now come back as themselves (25-27 Aug 2026).** Losing a session used to duplicate a person rather than lock them out, and every count in the group quietly stopped being true; four surfaces close that. Orbit asks for an email on the group home, ~~as a **note rendered for one viewer and never posted to the feed**, pinned just above the composer~~ (amended 27 Aug 2026, sheet redesign: it is a **bottom sheet over the group home**, still rendered for one viewer and still never posted to the feed. The reasoning is the counterintuitive part and it is why this is not an anti-clutter exception: the inline note was sticky until answered and competed with the chat feed for as long as a member stayed undecided, while a sheet takes the screen once and ends. **The founder's extra sentence is deleted**, so there is one string per ask for everyone, and the `viewerIsFounder` prop went with it. A promise line now sits under the field on both surfaces: "For sign-in and reminders. Never shared or sold." **This is the product's first modal**, so its focus handling, scroll lock, back gesture and ARIA semantics are the precedent, all four written into `EmailAskNote.tsx`'s header; drag-to-dismiss is deliberately not built. Only the worded exit spends one of the two lifetime asks: the scrim tap, Escape and navigating away are free, on purpose. The done step carries its own worded control, "Back to the group", added in review after the sheet was found to trap a member who had just succeeded: the same control-less done state that was harmless in an inline note becomes a trap behind a scrim and a scroll lock, which is the general lesson, not a detail of this element. **The sheet also outlives its own success**: attaching an address makes the server's offer null, which used to unmount the sheet mid-sentence, so a local latch keeps it open until the member closes it. And the taken-email dead end's way out is an outlined control rather than a quiet link, with the instruction moved out of the error copy and into it, after the owner walked straight past it on his phone). The group info page carries a permanent way in. The invite screen asks an unrecognised visitor whether they have been here before **before any account is created**, which is what stops the duplicate at its source. And `/signin` is a way back for somebody who no longer has the invite link, reachable from the front door. Sign-in is a typed **eight-digit code**, never a link, and the identity is upgraded in place rather than replaced. (~~True of the code. **Whether it is true in a member's inbox depends on a production setting nobody has checked**~~ **True in a member's inbox too, proven on production 28 Aug 2026 by sending: an attach and a `/signin` request from a second browser both delivered working codes, not links.** Original caveat kept for what it warned about: the sign-in email template must be switched to `{{ .Token }}`, and if it is left on the default link the decision is silently reversed while still working for whoever tests it. Build-notes' "After launch" item 4.3. **Narrowed 28 Aug 2026: the template is switched, and what is unproven is now only that it is the template this path renders.** It could not be proven before the merge, because `/signin` did not exist on the live site until this landed. The check is one send after deploy, and a wrong answer costs a dashboard setting rather than a deploy.) **Asked twice per person ever, then never again**: first on their first contribution of any kind (an RSVP, a chat message, or a gauge vote), second only after seven days **and** a fresh contribution. The counter counts declines, not appearances, so ignoring the ask is not answering it. **The three contribution lookups are scoped to the group whose home is being rendered**, so a member active in one group is not asked on another's screen, while the allowance stays per person because the counter lives on `User`; that is the thing the queued multi-group home would have to reopen. Wrong and expired codes are indistinguishable from the service, so one honest message covers both and no copy anywhere names an expiry. ~~**The address is never printed on any screen, not even to its owner**~~ (amended 27 Aug 2026, after the owner ran this on his phone and could not tell which address "Change email" was about: **the address is printed on exactly one screen, the group info page, to the viewer whose address it is, and nowhere else.** Fetched only for the viewer and never over the roster, so the rule's own reasoning, that the group never sees a member's address, is untouched. The data-model bullet below carries the amended rule), and a test now pins the reason the three whole-`User`-row pages are safe: `User` carries no email column, so adding one would leak an address onto three screens with no other edit anywhere. **Identities that already duplicated are not merged**, by decision; the founder's remove button is still the cleanup, and signing in does not recover a group only the duplicate ever joined. Riding this branch out of lane, declared: the shared header's hairline comes off the group home, group info and event detail, all three landing on a 14px gap, the feed's own seam hairline untouched. **Nothing in this slice has been seen rendered by anyone**, and every height figure is arithmetic. ~~**On merge day nobody but the owner can receive a login code** until the five production items in build-notes' "After launch" list are done.~~ **Overtaken 28 Aug 2026, merge day: the five were done before the merge rather than after it, so this was never true in production.** Resend, its DNS, custom SMTP, both templates and the domain settings are confirmed, and Secure email change is off on production as well. Two of them turned out to have been done weeks earlier without anyone realising, because **Supabase settings do not carry between the two projects** and every one of them had been set on dev-test; custom SMTP was already live on production, provable from the sending address on a QA code. **The whole list closed the same day, and a person who is not the owner can now receive a login code.** Reasoning, the notification-channel decision, the owner's build-time rulings and the lessons that generalise past this slice in build-notes §11. (Counts deliberately dropped from this pointer, 27 Aug 2026: it said "four rulings and the three lessons", the lessons section had been renamed to six, and the rulings were five once the taken-email one took its own section. A count inside a cross-document pointer is a fact about the other document's shape, which is exactly the kind that goes stale silently. The counted claims left in this paragraph are all claims about the product, and each was re-verified.)

**The digest has its plumbing now, and nothing runs through it yet (digest plumbing slice, 28 Aug 2026).** The app records when a member last opened a group, on `Membership.lastSeenAt`, written by `markGroupSeen` (`src/lib/groups/seen.ts`); nothing reads that timestamp yet, because deciding what counts as "unseen enough to mention" is slice two's job, not this one's. The product can now address mail through a second sending seam (`src/lib/email/send.ts`) that talks to Resend on its own `updates.` subdomain rather than riding the `account.` subdomain login codes already use, so a digest that ever collects spam complaints can never drag login-code delivery down with it. A guard inside that seam refuses to send outside production unless the recipient is named on a local allowlist, which is what stops a half-built feature running on a laptop from mailing a real person out of the dev-test database's QA rows. There is one unsubscribe door, per person rather than per group, sitting behind its own token that never touches `ContactMethod.isVerified` or anything else about sign-in, so opting out of digests can never accidentally sign somebody out or lock them out of their account. ~~**What is not true yet, stated plainly:** nothing composes a digest, nothing reads the read position to decide who needs one, no cron sends anything on a schedule, and~~ (struck 29 Aug 2026, digest slice two: all three are now built, and the paragraph below says what they are. Only the last clause survives, in a narrower form: no *digest* has been delivered to anyone.) ~~no email has actually been delivered to anyone by this slice~~ (struck 29 Aug 2026, Resend sending setup: true when written, and no longer. The rest of that sentence still holds; nothing composes or schedules a digest). The hand-run proof script's safer half ran and printed `suppressed_dev` exactly as the guard should; ~~the real send that would prove mail reaches an inbox could not run, because it needs a verified `updates.` sending domain in Resend and a production `RESEND_API_KEY`, neither of which exists yet, and setting both up is the owner's, now queued as items 7 and 8 in build-notes' after-launch list.~~ **Amended 29 Aug 2026: the owner completed the sending setup, closing after-launch items 7, 8 and 9, and the pipe is proven by a real message delivered to a real inbox** (`updates.` verified in Resend with Vercel writing the DNS itself again, two API keys named for the channel rather than the feature, the local allowlist set). Four things came out of it, all in build-notes §11. **The first message landed in spam**, which turns slice one's carried-forward caveat into a measurement and makes the product risk concrete: a digest that lands in spam does not fail, it silently does not work, so slice two should send to the owner and one friend before it sends to a group. **A DMARC record was added at the root domain** (`p=none`, monitor-only, so it cannot affect delivery), because none existed anywhere while SPF and DKIM were both already correct. And the one-click unsubscribe endpoint, dropped rather than built the previous day, is back on, reversed on today's evidence rather than on second thoughts. *(~~Queued 29 Aug 2026 with a trigger rather than a date~~ **Built 29 Aug 2026, digest slice two, tasks 1 and 2: the POST route handler exists at `/api/unsubscribe/[token]`, the one-click header is back, and the token is now race-safe so a link inside a sent email can never be orphaned by a second one minted moments later.** Original entry follows. Queued 29 Aug 2026 with a trigger rather than a date, the same form as the eval-bench gap that fired as written and the `ProposalSection.tsx` row on Orbit's speaking list. **Building it is the first task of the digest's slice two, or of whichever slice next touches `src/lib/email/send.ts`, whichever comes first, and it is two things together: the POST route handler that honours an unsubscribe, and the `List-Unsubscribe-Post: List-Unsubscribe=One-Click` header re-added alongside the plain `List-Unsubscribe` that already ships.** It is a trigger rather than a nice-to-have because the product landed in spam on its first real send, so without the button an annoyed member's only exit is the spam button, which damages the sending reputation of the very subdomain the `account.` / `updates.` split exists to protect.)* Evidence and reasoning in build-notes §11. **Debt this slice opens:** there is no send log anywhere in our own database, so once real sends start, the only record that a given email actually left the building lives in Resend's dashboard rather than in ours; if a member ever says "I never got it," there is nothing here to check against. Two honest limitations worth carrying forward: `updates.` starts with no sending reputation of its own, so its first messages may land in spam before that reputation builds; and the unsubscribe token never rotates, so forwarding a digest email to somebody else also hands that person the ability to unsubscribe its original recipient.

**The digest itself is built, and ~~no digest has been delivered to anyone~~ one has now been delivered, to the owner's own inbox (digest slice two, 29 Aug 2026; amended 31 Aug 2026 after his QA of PR #91).** ~~Both halves of that sentence are load-bearing.~~ **The correction, and the limit on it, in one place so neither half travels alone:** the owner ran the hand-run script against his own address on 31 Aug, the send returned `ok`, and the message landed in his Gmail **inbox** rather than spam, so a real digest composed by the real production path has reached a real person. **It is not evidence that `updates.` has earned any reputation**, because he clicked "Not Spam" on this sender after the 29 Aug test was filtered, which trains his own mailbox about this sender specifically. A friend's inbox is the only clean instrument, and **that send has not happened**, so the send-to-one-friend-before-any-group rule stands unchanged. A member holding a confirmed address now gets one email carrying two blocks: **what needs them** (an idea they have not voted on, an event they have not RSVP'd to, a time-change vote they have not answered), and **what they missed** (the count and the last three member lines since the later of their last visit and our last email). Either block having content sends an email; both empty sends nothing, which is what makes a daily cadence safe. The needs-you block is gated by two send rules, pure date arithmetic on rows already stored: the day a person created the thing (never a plan Orbit scheduled by itself, but yes to one a third yes created), and three days before it happens. When a rule fires the block carries everything outstanding, not just the trigger. The missed-chat block is ungated and stands on its own. At most one a day per member per group, in the hour the group's own clock reads 8pm, riding the existing hourly cron as a fourth step; `User.digestOptOutAt` means never again, anywhere. Alongside it: the members-only wall finally points at `/signin` instead of offering to start a second group, and one-click unsubscribe is real. No model call anywhere in the digest. **The group info page has promised reminders since 27 Aug and now has something behind the word**: its "Email for sign-in and reminders" eyebrow (`info/page.tsx:285`) and the assurance under every email field, "For sign-in and reminders. Never shared or sold." (`EmailAttachFlow.tsx:87`). Reminders become real in somebody's inbox the day this merges and the cron reaches a group's 8pm. *(The older phrasing of this claim named a line reading "Email reminders are on."; that copy was deleted on 27 Aug 2026 because it read as a setting with a switch behind it, and it survived in the design document and this file's queue notes for two days after it stopped existing in the product. Cited copy gets grepped.)* ~~**What has not happened, stated because this project has been burned by a record claiming a proof that never happened:** `npm run digest:test` composes a real digest through the real path and was exercised only as far as the environment guard, which suppressed the send. The real send is the owner's, to himself and one friend before any group, and it also answers whether the mail service rewrites the links in a body.~~ (Struck 31 Aug 2026: the send to himself happened and is described at the head of this paragraph; the send to the friend has not. **The link question is answered and closed: click tracking does not rewrite body links.** He read the full raw source of the delivered message and every link in it points at `https://interplanetarygroups.com/...` exactly as composed, with no tracking redirect anywhere.) **One decision is open and it is the owner's:** mailing a group means reading several members' addresses in one pass, which reddened the privacy guard whose own comment says a third read site gets a decision rather than a default; a narrow roster-scoped query was added and the guard amended to name and constrain it, as a proposal, not a ruling. **Debt:** still no send log; the once-a-day marker is check-then-write with no atomicity, so two overlapping cron runs for one group could in principle both mail; the missed-chat query is unbounded; and `updates.` still has no sending reputation, so the first digest may be filtered. ~~**Deploy obligation, on build-notes' after-launch list as item 10:** one migration must reach production before this merges, and slice one's two migrations were never listed there at all and should be checked in the same read-only `migrate status` run.~~ **Done 31 Aug 2026: all three migrations applied cleanly to production, so this branch's deploy obligation is satisfied ahead of the merge rather than after it. Slice one's two turned out to be genuinely missing rather than merely unrecorded, and they had taken the live site down for every signed-in member for four days, 28 to 31 Aug, while logged-out visitors saw a healthy site.** Build-notes §11, "the four-day outage nobody could see".

**The owner's QA of that PR falsified a recorded cause, and this is the part to read before anyone chases it again (31 Aug 2026).** On 29 Aug the record said Gmail showed no unsubscribe control because the one-click header was absent, tagged as well supported but not certain, and recommended slice two build the endpoint and re-add the header. Slice two did exactly that. The delivered digest provably carries `List-Unsubscribe-Post: List-Unsubscribe=One-Click` with both unsubscribe headers inside the DKIM signed-header list, so nothing stripped them, and **Gmail still shows no control**. The recorded cause was wrong. Current best explanation: Gmail reserves that control for senders it recognises as bulk by volume and reputation, and this domain has sent a handful of messages ever *(plausible, and unverifiable from outside Google, so treat it as the working guess)*. **The work was still right and should not be read as wasted**: the header is required by the mailbox providers' own bulk-sender rules, it is what makes one-click work in clients that do honour it, and the product's own "Stop these emails" link in the body is the exit that always works and did render. Nothing further should be attempted from the header direction. Three visual changes came out of the same QA (commit `e64e057`): on the members-only wall "Been here before? You can sign in with your email" is bold, because as flat paragraph text it was the sentence people would skim past and it is the one that gets a returning member back in as themselves; "Sign in" is now a teal button while "Start your own group" stays a quiet text link, which is **the colour rule being followed rather than bent**, since two equal-weight text links told a member who had just lost their session that starting a second group weighed the same as recovering their own; and, declared out of lane, `/signin`'s "Never mind, take me back" link was centred at the owner's direct ask. Settled by his eye on the real rendered email and closed rather than carried: the 100-character quote truncation reads fine on a phone.

**The product can say it is broken now (site health monitoring, slice one, 1 Sept 2026).** The hourly cron finishes by running a read-only self-check over the reads a signed-in screen depends on (a whole `User` row with no `select`, a whole `Membership` row, and the group home's own queries against the most recently active group), then reports the verdict to **Better Stack** as a heartbeat: healthy pings the URL, broken pings `/fail` with the reason, and **nothing arriving at all is itself the alarm** after a grace period, which is what covers a dead deploy, a stopped cron, and the free-tier database pause. The alarm is deliberately not an email from Orbit: an alarm inside the thing it watches goes quiet exactly when that thing dies, and the owner's noise budget (~~immediate, then six-hourly, then an all-clear~~) needs state this app cannot hold without a migration. *(Corrected 1 Sept 2026, from the dashboard rather than from the design conversation: **there is no six-hourly repeat and no way to buy one on this plan.** Better Stack's only follow-up setting escalates to other team members, of which a one-person account has none, so what the owner actually gets is one email when an incident opens and one when it resolves. Quieter than the budget he specified, accepted on his call: one email per outage cannot cry wolf, and the open incident in the dashboard is the backstop against missing it. Build-notes after-launch item 13.)* It **detects** the free-tier pause and does not prevent it, which is a hosting decision left out on purpose. Two things it deliberately does not cover: render bugs and one-bad-group crashes, which the Vercel log drain into Better Stack takes next (dashboard configuration, no code); and **until `HEALTH_HEARTBEAT_URL` is set in Vercel (build-notes after-launch item 12, with the monitor's settings at item 13) the product is still blind**, because nothing is sent. ~~Nothing here has been seen working in production and no incident has ever reached the owner's inbox.~~ *(Amended 1 Sept 2026, the owner's QA of PR #100, and the two halves of that sentence now have different answers. **An incident HAS reached his inbox**, raised by a real `/fail` ping from his laptop and delivered by Better Stack, so the last link in the chain is proven rather than assumed. ~~**Nothing has been seen working in production**, which is still exactly true: the Vercel cron has never run this code, and it cannot until `HEALTH_HEARTBEAT_URL` is set there.~~ *(Overtaken the same evening, 1 Sept 2026, about two hours later. The owner created the production heartbeat, set the variable in Vercel and redeployed, and **the Vercel cron then ran this code in production on its own schedule and reported healthy**: Better Stack shows a received heartbeat at the top of the hour and zero incidents, with no laptop able to have sent it, since the local `.env` value had been deleted first. That is worth more than "the wiring works", and it is the part to carry forward: **the check ran against the production database and every probe passed**, so production's schema matches what the code expects. That question had been open since 24 Aug 2026, recorded at the deploy entry as whether all fourteen migrations apply cleanly, and it is now answered by the product itself rather than by anyone looking. What is still unproven, and cannot be proven without a real outage: that the alarm fires **from production**. The alarm path was proven from the owner's laptop, and the two halves together are the most that can be shown honestly.)* Also learned by looking rather than reasoning, and recorded at after-launch item 13: **the notification email carries no diagnosis at all**, only the heartbeat's name, "Reported failure" and a timestamp. The step name, the error code and the cause are complete on the incident page, one click away. The alarm is two-step by design; nobody should expect to triage from the inbox.)* *(Amended 1 Sept 2026, whole-branch review of this same slice: the "two things it deliberately does not cover" list above **reads exhaustive and is not**, and this matters because this file loads every session, so a future session would otherwise believe the gap closes the day the log drain is configured. **Two further classes are covered by neither the check nor the drain.** First, **a table the product only writes to, or only reads with a `select`, is invisible to every probe**, because each probe reads a whole row and nothing else column-checks a table. The live example is `ContactMethod`: all three of its reads take a `select` (`src/lib/auth/email-ask.ts:27` and `:57`, `src/lib/digest/run.ts:184`), so a missed migration on that table would break email attach and sign-in while all three probes stayed green. Second, **Supabase auth fails soft**: `getCurrentUser()` reads only the `data` half of `auth.getUser()` and returns null when the call errors, so a degraded auth service or a rotated signing key logs every member out quietly onto the front door or the members-only wall. Nothing throws, so there is no 500 for the drain to see either, and no probe touches Supabase at all: a green light over a product broken for every member, the same shape as the outage this slice exists for. (Confident about the code path; not certain supabase-js never throws on every auth failure mode, so treat the second class as very likely rather than proven.) No code change was made for either: adding a Supabase probe is a scope decision for the owner, not the reviewer's. Reasoning in build-notes §11.)*

~~**Site health monitoring is queued, not built, off the outage above (31 Aug 2026).**~~ **Built 1 Sept 2026; the paragraph above says what it landed as, and build-notes §11, "site health monitoring, slice one, the checklist", carries the reasoning. Entry kept for the diagnosis it records, which is still the reference for why a plain uptime ping was never the answer.** Original follows. The product was down for every signed-in member for four days and nobody caught it until the owner happened to open the app; nothing in this product currently reports that it is broken. The failure's shape rules out the obvious cheap fix: **a logged-out visitor saw a perfectly healthy site the whole four days**, because the front door, join screen, and sign-in screen never read a `User` row, so a plain uptime ping would have stayed green throughout. Whatever gets built has to exercise a signed-in path, or watch for runtime errors directly, rather than just checking that the site responds. Unranked and the owner's to place, behind the three items he already ordered below plus the README rewrite and the second-group entry point. Reasoning in build-notes §11, "the four-day outage nobody could see."

**There is a way into a second group now, and the control that was supposed to be it has stopped giving a wrong answer (second-group entry point, 1 Sept 2026).** `/groups` is a new screen listing the groups you are in, most recently opened first with never-opened ones last, carrying "Start a new group". **Tapping the Orbit mark in a group's header always goes there**, whatever number you belong to, because a tap is a spent choice rather than a tax on arrival. **Arriving at `/` cold still guesses, and that is deliberate**: no groups sees the pitch, exactly one goes straight in with no tap, several sees the list. The two destinations were welded together and pulling them apart is the whole slice. **This repaired an active defect, not only an absence:** the mark pointed at `/`, which redirected to your most recently joined group, so a member of two groups tapping it either bounced straight back where they already were or was silently dropped into a different group. **The scope line is firm and it is prose, not a mechanism:** the screen says *which* groups you are in, never *what is happening* in them, so no counts, no next-event line, no badges. The multi-group home stays out of scope. Two design elements were built and then dropped by the owner, both recorded in build-notes §11 with the reasoning: the lime initials emblem (two group names in the same list derived the same initials, so the name does the telling anyway) and the scroll fade (its cue is already carried by the next row peeking up, and making it work needed a constraint that would have rendered the whole list invisible if a future page forgot a definite height). **No migration, so no deploy obligation and nothing for the after-launch list.** **What has and has not been seen:** the browser pass at 375x812 is the first time the several-groups path has ever been seen rendered, since it had been covered by unit tests alone since July, and it confirmed the ordering is live rather than static and that a long three-word group name wraps rather than clips. Not seen: the zero-groups arrival (the QA session had groups) and the list overflowing its scroll region (six groups fit an 812px screen with room to spare). **The real-phone pass is the owner's and has not happened**, and the one question only it answers is whether the "Start a new group" control is findable; it is deliberately quieter than a group row, and a no there is a finding rather than a failure. Two changes ride this branch out of lane, both declared: a pre-existing dangling timer in `ShareInviteLink.tsx` was fixed because a safety-net hook blocked on it, and `.claude/launch.json` gained a `dev-webpack` entry because Turbopack refuses a worktree's `node_modules` symlink, which meant no worktree in this project could start the dev server at all.

**The product is ready for other people's data, and a person can now actually be deleted (1 Sept 2026).** There is a privacy notice at `/privacy` and terms at `/terms`, ~~linked in four places: a quiet footer on the front door and the group info page, and a consent line beside the action at the two moments somebody actually hands something over, joining a group and starting one~~ **linked in five places: a quiet footer on the front door, the group info page, and `/groups`, plus a consent line at the two moments somebody actually hands something over, joining a group and starting one** (amended 2 Sept 2026, owner's phone QA: `/groups` had no footer, and it is where a signed-in person with a group actually lands, since `/` redirects them there; it is also why he could not run the front-door step of his own QA script as written). The notice says what is collected, who can see it (including, plainly, that the person who built this can read the database), which outside services handle it, that its cookies only keep you signed in with no tracking, and how to be deleted. **Deletion is by request, at `privacy@interplanetarygroups.com`, within seven days, and never a button in the app**, for a reason that is a missing key rather than effort: the app holds no key able to delete a Supabase login, so a self-service button would wipe somebody from our database, leave their login alive, and let the next tap on an invite link recreate them as a brand-new person. The owner runs `npm run person:delete`, which prints a plan in plain language and makes him type the person's name before it writes anything, then finishes the login by hand in the Supabase dashboard; `docs/runbooks/person-deletion.md` carries the rest, including the founder-handover case that was deliberately left unscripted because it is rare, and rare is when a runbook earns out. What deletion does and does not take is in the data-model section below. Riding alongside it: somebody who unsubscribed from digests by accident now has a way back on the same screen, where before nothing anywhere ever turned them back on. **Amended 2 Sept 2026, the owner's phone QA (`7f211fe`, `fbb8c0a`):** the notice took a brevity pass, about ninety fewer words, no caveat cut, and now separates the operator's read access from deletion itself, which is mechanical and not an occasion to read anyone's messages; the same sentence now also covers investigating a reported problem, matching a promise the terms already made and the notice had not. The terms gained a liability section (not responsible for what members do to each other, a breach is possible despite reasonable care, provided as is) and a real mailto for `privacy@interplanetarygroups.com`, so after-launch item 14's mail forwarding now blocks two pages' promises rather than one. *(Closed 2 Sept 2026: forwarding is live through ImprovMX and proven by a real third-party send reaching the owner's inbox. After-launch item 14 is done. Reasoning and setup in build-notes §11.)* Consent links open in a new tab so reading them never costs a half-typed group description; create step 1's consent line is back above the fold (the description field itself is untouched, still `rows={5}`); and the join screen dropped its untrue no-password line. Reasoning and the full list of copy fixes in build-notes §11.

**What that slice measured rather than built, and what it leaves open.** **Sign-in codes cannot be guessed, and it is Supabase's own rate limit that stops it, not our code.** A hand-run probe against dev-test hit a wall at the 31st attempt (30 wrong codes processed), then at the 10th on a retry seventy seconds later, both 429s. Read that as a setting we are relying on rather than a defence we wrote: our code contributes zero attempts-based resistance, and there are still two doors into the same seam (`src/app/actions/signin.ts` and `src/app/actions/join-signin.ts`). Dependency advisories went from 14 (9 high, 5 moderate) to 4 on the safe fix; the four left are reachable only through the Prisma CLI (a MySQL driver in a Postgres product, and a config parser), neither loadable by the deployed app, and the forced fix was declined because it downgrades Prisma 7 to 6. **`robots.ts` leaves `/privacy` and `/terms` indexable**, untouched deliberately: the standing decision is that search engines get the front door and nothing else, and these two pages sit on the other side of it, so opening them or not is the owner's call. *(Decided 2 Sept 2026: leave it exactly as is, nothing changes. His real question was about spam, not indexing, and blocking these two pages would not buy non-discoverability anyway since the front door is already indexable and is what makes the product findable; the legal pages carry no member data. Indexing has no effect on email deliverability, a live misconception this settles. Reasoning in build-notes §11.)* **A Jacob-built guard was changed, declared:** `token-contrast.test.ts` gained two entries, after verifying both new components render only on `--surface-base`. **Debt:** the privacy notice will go stale silently, because nothing makes a future slice that adds a field or a vendor update the page, and this pointer is the strongest guard available and not a strong one; the deletion script's join-line lookup is a hand-judged name match whose weakest class can miss a genuine silent leaver, since somebody who joined, never posted, never voted and then left leaves nothing to match on; the founder-handover query has never been run against real production data; and whether the session-pooler connection can be reused for a production deletion run is a judgement made while writing the runbook and flagged inline there. **No migration, so no deploy obligation.** ~~**The owner's, before the tennis group arrives:** `privacy@interplanetarygroups.com` needs real mail forwarding, because ~~the notice names~~ **both the notice and, since 2 Sept 2026, the terms name** an address that does not receive mail until he sets it up (after-launch item 14).~~ **Done 2 Sept 2026: forwarding is live through ImprovMX and proven by a real third-party send reaching the owner's inbox. After-launch item 14 is closed. Reasoning and setup in build-notes §11.**

~~**Registered 1 Sept 2026, not this slice's to fix: two pre-existing test flakes, and they matter more than a footnote.** `OrbitNoteScreen.test.tsx` (failing with "window is not defined") and `UnsubscribeForm.test.tsx` each failed once under the full suite and passed in isolation and on rerun. Both are weeks old, neither was touched by this slice, and the shape reads as vitest environment teardown rather than a product defect. It earns its own item because an intermittently red suite erodes the baseline number this project's whole verification process rests on: a failure with no owner is exactly what the record-the-baseline rule exists to prevent. Recommended as its own micro-PR, unranked, the owner's to place.~~ **Fixed 2 Sept 2026, `fix-testing-library-cleanup` micro-PR, and the reading above was wrong in the way that matters: it was never a race between files.** Testing Library's automatic cleanup registers itself only when the runner puts `afterEach` on the global object, and this repo runs vitest with `globals` off on purpose, so **auto-cleanup had never once run here** and every `render()` left its tree mounted for the rest of its file. A leaked tree can still hold a deferred passive-effect flush in React's scheduler, which prefers Node's `setImmediate`, and when that callback fires after the file's jsdom environment is disposed it reads a `window` that is gone; vitest then blames whichever file was running rather than the one that queued it, which is why the named file was usually innocent. `vitest.setup.ts` now unmounts and drains after every test, wired through `setupFiles`; `globals: true` was declined because it makes `describe`/`it`/`expect` ambient across 139 files that import them by name. **The evidence is the mechanism, not a run count:** each guard in `src/__tests__/testing-library-cleanup.test.tsx` was shown red with the thing it guards removed and green with it restored, and a green suite is deliberately not offered as proof, since this went green three runs running right after the failure that started it. **What that guard holds is narrower than it looks, and the file says so:** the behavioural half holds the unmount only, because deleting the drain leaves the whole repo green; the drain is covered structurally instead, since a behavioural probe for it would pass for the wrong reason whenever the runner turned the event loop between tests. Reasoning in build-notes §11, "the component-test flakes had one cause".

**Registered 2 Sept 2026, a different shape of the same class, not this slice's to fix: a pre-existing test failed on its own with nobody changing code.** `src/lib/digest/__tests__/run.test.ts`'s "missed chat" case pinned a message to a hardcoded instant but left the recipient's join time unset, so Postgres stamped it with the real clock; the two only stayed in order by luck, and the test began failing at noon UTC on 2 Sept 2026 and would have failed forever after. Not a flake: deterministic, and confirmed to fail on `main` too. Fixed on its own branch, `fix-digest-test-wall-clock`, ~~**not yet merged**, so the suite on this branch and on `main` both still carry it red until the owner merges that fix~~ **merged same day as PR #106 (`929a670`): `main` is at that commit, this branch is rebased onto it, and the full suite reads green, 1576 passing across 139 files, zero failures** (confirmed by running it). Same lesson as the empty-database rule: a test leaning on ambient state it does not control.

**A plan can be called off now, and put back (cancel one occurrence, 2 Sept 2026).** Anyone in the group, never the founder alone, calls off a single occurrence from that plan's own page in two taps, and anyone can put it back the same way; weather is an observable fact, and founder-only would rebuild the organizer role the product exists to dissolve. Orbit announces it naming the person who did it, which is deliberate and is the only Orbit announcement that names anyone: a time change goes through a group vote so it is nobody's individual call, while a cancellation has no vote and no permission gate, so the social check is the only check there is. **Cancelling sets a status and never deletes the row**, and that is load-bearing rather than stylistic: `hasUpcomingScheduledEvent` has no status filter, so a delete would free both that guard and the unique `scheduledKey` and the hourly cron would recreate the cancelled plan within the hour with a fresh announcement. The retained row is its own tombstone and reconcile needed no change. **RSVPs survive untouched**, which is what makes "anyone can undo it" safe rather than merely permitted, and it is why the restore announcement can say "Everyone's RSVPs are the same as before" truthfully. A called-off plan still appears on the group home rather than vanishing: it reads as called off, dropping its counts and its RSVP pair, while the detail screen keeps its roster, and the two disagree on purpose (the card shows the gist, detail carries completeness). Status is carried by brightness and a grey "Called off" label, never by hue. The digest stopped nagging people to RSVP to a game that is off, and now tells a member what got called off while they were away, derived from event rows rather than by widening the you-missed block's MEMBER filter. **The one that mattered most to the tennis group:** a called-off plan used to silence Orbit for any new idea about the same activity, so a group could not reschedule the game they had just called off; that guard was the same query written twice and is now `hasUpcomingEventForActivity`, extracted and tested. **Deliberately still missing, each its own feature:** proposing a cancellation for the group to vote on ("only four of us, should we skip it?"), the immediate cancellation email, and Orbit hearing "rained out" in chat. **Known and not fixed:** a member who already saved the plan to their calendar still gets buzzed, because "Add to calendar" is hidden on a called-off plan and there is no path to a corrected file; the subscribable feed in build-notes §6 is the real fix. Reasoning, the race the plan missed, and the two untested seams in build-notes §11.

**Amended 2 Sept 2026, the owner's phone QA of the cancel slice, before it merged.** He found a bug nobody else had: `planChange` declined a day change BEFORE checking whether any plan existed, so a group whose plans were all called off got "I can't move it to another day yet, I can change the time if that helps", offering to change the time of nothing. Pre-existing, fires in an empty group too, and cancellation made it common; the ladder now asks "is there anything to move" first. Orbit's announcements lead with the state and shout it ("Squash this Thu is OFF... Anyone can put it back on, tap the plan up top"), because the short words were missable and the old wording pointed at a page without saying where. **The cancel and restore controls left the details card**: they had been inside the footer band that belongs to the RSVP pair, which made a box inside a box, a control narrower than the pair above it, and a confirm step that read as four options for one question. They are full-width pills below the card now, restore teal (on a called-off plan it is the screen's only real action) and cancel outlined (the RSVP is still the primary ask). The confirm's brightness flipped so the destructive option is no longer the louder one. "CALLED OFF" moved above the title on the card; **a filled chip was built, measured 4.2px over the card region's height budget, and was pulled**, because that budget is a hard constraint and the chat feed pays for any growth. The duplicated lowercase activity row on event detail is deleted. **And the double-tap on event cards, which he reproduced on production, had three causes**: the tappable link covered only part of the card (51% of a called-off card was inert, its bottom 43% completely dead), the carousel rail is a two-axis scroll container iOS may spend a first touch on, and **nothing in this product has a pressed state** while navigation takes about a second. All three fixed, inert area 50.6% to 1.1%, hit-tested; **two of the three target iOS and nothing has touched an iOS device**, so they rest on documented WebKit behaviour. Reasoning in build-notes §11's postscript.

**Amended 3 Sept 2026, the owner's second phone QA.** The "CALLED OFF" label is bright white on both surfaces now, which is a rule and not a patch: the card carries status in brightness and a called-off card dims its title, so the label being the brightest thing on a dimmed card inverts the hierarchy on purpose. **The word "called off" stayed**, against a suggestion to switch to "cancelled", because the owner was solving a contrast problem with a vocabulary change; he missed the label rather than misreading it, and grey at 13px is equally missable whatever it says. The two confirm buttons went back to **equal weight**, reversing a lean I had recommended two rounds earlier: the product's own rule is that an open question does not lean, and the safe-first position already does the accidental-tap protection. The group info page's "Want to change something? Just tell Orbit in the chat." is **deleted for being false** rather than for being clutter, since name, members, rhythms and venue all get an honest decline. **The tap freeze he saw after a restore was NOT reproduced** across five attempts including the exact race; the instrument is wrong (he was on iOS Safari, the pane is Chromium) and the leading candidate predates the tap fixes, so nothing speculative shipped. **Confirmed while looking for it: the email-ask sheet reappears on every return to the group home while unanswered**, which is working as designed and the design is wrong, since only the worded exit spends an ask. Own micro-PR, ranked high. Reasoning in build-notes §11's second postscript. **Built 3 Sept 2026; the paragraph below says what it landed as.**

**The email ask takes no for an answer now (3 Sept 2026).** Tapping outside the sheet, which is the natural "not right now" gesture, used to record nothing, so a member met the sheet again on every single return to the group home, forever. It now buys **24 hours of quiet**, and so does every other way out. **The two lifetime asks are untouched**: only the worded "Not now" still spends one, tapping outside still spends nothing, and that asymmetry is still deliberate. Carried in a cookie, per person rather than per group, so no migration and no deploy obligation. Accepted knowingly, on the original design's own reasoning: somebody who only ever taps outside is asked once a day indefinitely, because tapping away usually means they did not read it rather than that they refused.

**Two things that slice decided by measuring rather than reasoning, and both changed the design.** First, before any code: **the phone's back gesture does not re-run the server render and cannot be made to.** Next's client router cache serves back and forward navigations from memory, does not expire (confirmed by waiting forty seconds), and is not configurable. So a server-side cookie read alone would have been honoured on every path except the one an iPhone user reaches for first, and **the sheet therefore reads the cookie itself, exactly once, at the instant it is created.** That reverses an instruction the owner gave deliberately; he reversed it himself once the measurement was in front of him. The narrowness is the safety: looking once, at birth, means it can never hide itself while somebody is reading it. Second, the slice introduced and caught a real regression, which is the thing to know before touching this file: **giving the sheet a cookie gave it a way to silence its own props**, and Orbit answering a member's first message revalidates the group home a second later, so the sheet vanished under the reader. The `attachedUnder` latch was widened from "attached" to "shown" and now covers both, proven by mutation rather than argument, and **`attachedUnder` is gone**; do not go looking for it.

**Verified in a browser at 375x812, not just in tests:** the sheet appears and writes the cookie, tapping outside then returning through the app is quiet, the back gesture is quiet, deleting the cookie brings it straight back, and group info's permanent email row is untouched. `emailAskCount` was still 0 after every free exit. **Not verified: a real iPhone**, since the pane is Chromium. Debt, all small and all in the spec: per device and clears with site data; a shared device inherits one snooze; two places know about the cooldown, which looks redundant and is not; the parser fails toward showing while the mount check fails toward silence, reachable only by hand-editing a cookie; and a tab left open past expiry waits for the next mount. Reasoning in build-notes §11.

~~**Registered 3 Sept 2026, not that slice's to fix and the record should stop implying the class is closed: `UnsubscribeForm.test.tsx` still fails intermittently**~~ **Fixed 3 Sept 2026, same day, and the diagnosis below turned out right after surviving what looked like a refutation: 26 clean runs and a purpose-built probe all failed to reproduce it, and what settled it was recovering the morning's actual error text (`expected true to be false`) rather than reasoning further. Two tests, not one, read a transition-driven `disabled` flag on the line after an `await findByText`, asserting a coupling React does not guarantee; both now poll, and each was proven non-vacuous by mutation rather than by a green run. An unreproduced failure is not a disproved one. ~~Separately and NOT fixed: the safety-net hook and a subagent's own suite run can both run the full suite at once against this project's single dev-test database, which produces Prisma transaction timeouts naming innocent files and reading exactly like real defects; re-run alone before believing a red suite from that gate.~~ **Fixed 4 Sept 2026 for the case it names, and NOT for the one it does not.** `suite-lock.mjs` is adopted from the safety-net template and wired as a vitest `globalSetup`, so two runs **inside one checkout** now serialize: proven by a second run waiting 59s instead of its usual 0.2s and then succeeding, not by a green suite, which passes with the lock absent too. **It keys on the working directory, so two git worktrees sharing one dev-test database still do not block each other**, and asking the other session before taking the database stays the only protection there. The finding worth more than the fix: the template file had never been run against this repo, and wiring it as written **deadlocked the suite**, because `run-tests-unless-docs.test.ts` spawns a nested vitest that waited 300s for a lock its own parent held. `safety-net-drift.mjs` can say a file is missing; nothing said it had never been executed. Both in build-notes §11.** Original entry follows.**, on that branch and on main, untouched by it, seen twice by two different agents in one day. Diagnosed as a `disabled` read with no `waitFor` racing a two-commit React update, which is a **different mechanism** from the auto-cleanup cause the 2 Sept `fix-testing-library-cleanup` micro-PR closed in the same file. Its own micro-PR, unranked.

**The group home keeps itself current now, and the bug that forced it was found by two people using the product at once for the first time (4 Sept 2026).** The owner's wife joined, posted twice, Orbit answered her, and his own open phone showed an empty room until he reloaded. **Nothing in this product updated an open page**: no polling, no socket, no realtime subscription, and no refetch on focus or visibility, all four searched and confirmed absent. `revalidatePath`, which this codebase calls everywhere and whose comments read like a live-update mechanism, invalidates the server cache for the *next request* and pushes nothing to anyone's open tab. So a member who simply sat and watched saw a frozen room, forever. `LiveRefresh` (`src/app/groups/[id]/LiveRefresh.tsx`, modelled on `SeenMarker`) now calls `router.refresh()` every ten seconds while the tab is visible and on tab return, paused while a send is in flight. Whole-screen refresh by decision, not messages-only: chip votes and RSVPs post no chat message, and **a called-off plan must never keep reading as on**. Silent on failure, no indicator, group home only. **Supabase Realtime Broadcast is the better end-state and is declined on cost against evidence rather than on principle** (it ships the anon key to browsers for the first time and needs channel authorization that fails open and silent); revisit trigger, members chatting live rather than coordinating. **This was a QA gap, not an architectural mistake**, and the record says so: the fix is thirty lines, and every QA pass in this product's life until that night was one person on one phone. `npm run qa:stage-chatsync` plus `--say` makes it a one-person test from now on, which is the durable output. **Three things review caught that would otherwise have shipped:** a comment claiming the `try/catch` stopped a rejection reaching `error.tsx` when `refresh()` returns void and cannot (settled by reading Next 16.3.2's source: no gap existed, the claim about it was wrong); `paused` bound to the raw `useOptimistic` list, which on a hung send never clears, so refresh would have paused **forever** for that tab and silently restored the bug; and a scroll hijack this slice introduced, whose first fix then failed on an empty feed, which is every brand-new group. **The suite cannot distinguish the two `paused` bindings** (jsdom has no real router transition), so a future revert would pass CI and fail only in a browser; the tests guard the counter's wiring and the choice rests on the promise-semantics argument in `GroupHome.tsx`. **Debt:** every visible tab now costs a full server render every ten seconds, affordable here and unmeasured at any other size, with the connection pool the thing to watch; and **the digest now drifts**, because `lastSeenAt` is written on mount and seeing messages no longer requires a reload, so a member who leaves the tab open across the cutoff is told they missed messages they watched arrive. Reasoning in build-notes §11.
**Onboarding's exit stopped guessing, and its name field stopped lying (4 Sept 2026).** Two defects the owner found in production. `/create` step 1's "Never mind, take me back" pointed at `/`, which is the **session-aware front door, not a neutral home**: `resolveFrontDoor` sends a member of exactly one group straight into that group, so backing out of creating a group deposited you inside your existing one. It points at `/groups` now, correct in every reachable case rather than usually, since `/create` is reachable only from `/groups` or from `/`, and `/groups` redirects a zero-membership visitor onward itself. And step 1 asked every founder for a name, gated Continue on it, and **silently threw away what a returning founder typed** (`provisionFounderGroup` reuses the existing `User` row and writes `name` only when creating one) while `Step2Playback` echoed it back as YOUR NAME, so somebody could type Bob, watch Orbit confirm Bob, and create the group as Jacob. It is now prefilled from the session and rendered as text, never a disabled input, with Continue gated on the description alone; a first-time founder sees exactly what they saw before. **Editing was declined by the owner on blast radius, not effort:** `name` lives on `User`, not `Membership`, so an edit there renames the person in every group they belong to, retroactively, on every past message, from inside a screen about starting a different group. **Accepted cost: name editing exists nowhere in this product**, so a returning founder whose stored name is wrong sees it here and cannot fix it; it belongs on the group info page or in the queued founder-edits-group-details slice. Review caught one accessibility regression whose shape matters more than its size: with the name read-only the label lost its programmatic tie to the value, and **the test asserted that absence as expected behaviour**, so it would have passed forever while the accessibility stayed broken. Fixed with `aria-labelledby` and the assertion inverted. No migration, no deploy obligation. Reasoning in build-notes §11.

**A refresh can no longer pile up behind a slow render, and the bug it fixes was predicted before it shipped (4 Sept 2026).** Hours after the chat-sync slice landed, the owner opened a phone to a tab that had sat on the group home while his wife sent five or six messages: nothing arrived **and the whole app was dead**, taps, navigation and sends alike, until he reloaded. `LiveRefresh` fired every 10s with no in-flight check, and **Next runs every server action and every refresh through one strictly serial queue**, so refreshes outlasting their interval stacked and everything queued behind them while the optimistic layer kept the page looking alive. Now at most one refresh is in flight and a trigger arriving during one is **dropped, never queued**, so a tab that slept through fifty ticks owes one refresh rather than fifty. The signal is `useTransition`'s pending flag, and that it genuinely tracks the RSC round trip rather than clearing at dispatch was established by two independent readings of Next 16.3.2 and React 19.2.4 source; had it cleared early the guard would have been a placebo. **The flag is bounded at three intervals**, because review found it had exactly one exit and a hung fetch would otherwise kill polling for the life of the tab, which is the second unbounded-flag defect found in a single day on this product. **The sentence to read before chasing a recurrence: this stops refreshes piling up, but one refresh that hangs still wedges the queue and the tab**, so if the dead-app symptom returns, doubt the assumption that pile-up was the only way the queue got stuck. Reasoning in build-notes §11.
**The composers wrap now, and Enter no longer sends (4 Sept 2026).** Both message inputs were single-line `<input type="text">`, so a long message scrolled sideways and the member could not see what they typed; the group chat and onboarding's gap-ask are both `<textarea>`s now, growing to about five lines and then scrolling. **Enter inserts a newline and the send button is the only way to send**, the owner's overrule of this slice's own spec, matching iMessage and WhatsApp on a phone; the consequence is that the send button is now the only exit from both composers. Two things worth carrying: a `<textarea>` is not a drop-in for an `<input>`, because `scrollHeight` excludes the border even under `box-sizing: border-box`, which left the bordered gap-ask 1.5px short until it was measured in a real browser (resting heights now 26px unchanged for the chat, 43.5px to 44px for the gap-ask, a rounding artifact reported rather than smoothed); and **jsdom implements no implicit submit-on-Enter for any control**, so the "Enter does not submit" test would pass against the old input too and is conclusive only paired with its sibling asserting the element is a textarea, which is what its comment now says. Reasoning in build-notes §11.

**The venue stopped hiding on onboarding step 2, and it is required now (4 Sept 2026).** An empty venue was a small underlined link under the schedule row, easy to miss, and tapping it revealed a 14px input that **iOS Safari force-zooms and never zooms back**. It is now a full-card-width control in the group-name field's own visual language, at 16px so the zoom cannot happen, with "(optional)" dropped. **This partially reverses a 22 July decision** that made the empty state a quiet link, and the part reversed is its quietness, not the principle behind it: an empty box is still not an input, because the group-name row edits something Orbit produced while an empty venue collects something it did not, on a card headed "Here's what I understood." What changed since July is that **a missed venue is permanent**, since nothing in this product can add one after creation. **The primary rhythm's venue now blocks confirm**, which narrows the standing venue-never-gates rule rather than reversing it: the model still never blocks anyone, only the founder's own empty box does, and **it reverts when the editable event card ships**. Secondary rhythms stay optional on purpose, not for effort: a monthly beers often has no fixed place, and a forced "idk" would propagate onto real events through venue inheritance, where this project already prefers no venue to a wrong guess. **And the gap-ask card shows a captured venue at last**, which was the actual defect behind the report: extraction captures it and step 2 displays it, but `StepGapAsk` renders through `PlaybackCard`, which had no venue rendering at all. Reasoning in build-notes §11.

**A deployed fix does NOT reach an open group home, and the slice that claimed it did was wrong (4 Sept 2026, corrected the same day, in production, before anything was built on top of it).** ~~A deployed fix already reaches an open group home, and nothing in this project configured that.~~ **What is true and unchanged:** Vercel supplies a deployment id with no configuration from us, for any Next project created after Nov 2024, and **`next.config.ts` must never grow a `deploymentId`** because a hand-set value need not match what Vercel assigns at deploy time, which is the one change that could reload every tab forever. **What is false:** that an open tab therefore picks up a new build within ten seconds. **Vercel's Skew Protection pins each tab's framework requests back to the deployment that tab booted from**, which is exactly what it exists to do (version locking, so old clients keep working). So the poll's response always reports the id the tab already has, the mismatch Next would react to never occurs, and **`LiveRefresh` never triggers a reload for a deploy.** Proven in production by redeploying while a phone sat open on the group home: nothing happened, and a request carrying the **previous real** deployment id came back answered by **that old deployment**, on all three of the header, query and cookie forms. **The reusable lesson, which is why this is recorded at length rather than quietly fixed: the probe that produced the wrong answer used a BOGUS deployment id.** A deployment that does not exist cannot be pinned to, so Vercel fell through to the current build and the 200 read as proof of no pinning. **Only a real, still-existing previous deployment id can detect pinning at all.** A probe has to be capable of returning the answer you are afraid of. **What survives from that slice:** the traced Next mechanism (`fetch-server-response.js:175` through `app-router.js:221`) is correct, and the local four-build experiment was sound; it simply ran without Vercel's routing layer in front of it, and **a local `next start` cannot falsify a claim about Vercel.** **And the composer's parked draft keeps its whole justification, on a different reason than the one it shipped with:** the poll's own failure path hard-navigates on a **dropped connection**, pinning or not (`fetchServerResponse`'s catch logs "Falling back to browser navigation"), which is the common case on a phone and far more frequent than a deploy. Reasoning in build-notes §11, and the correction in its postscript.

**Next, per the pre-MVP triage order (10 Aug 2026, amended 11 Aug), do not re-derive:** ~~the pre-launch whole-codebase audit registered in §8, then the deploy checklist~~ (amended 23 Aug 2026, pre-deploy-fixes slice: the audit landed on main at `d6e4ecb`, and this branch is the fix wave that came out of it). ~~**Next is slice B, the deploy itself:** no code, run against main, working the "Before first Vercel deploy" checklist in build-notes §11 from the top.~~ (Done 25 Aug 2026.) ~~**Next is email sign-in**, settled with the owner, because it cannot be tested without a live site and because until it exists a member who loses their session rejoins through the invite link as a second person and every count is quietly wrong.~~ (Built 25-27 Aug 2026, this branch; the paragraph above says what it landed as. ~~**Nothing is settled as next**~~ **The next three are settled and ordered, by the owner, 28 Aug 2026, and the order is his rather than a build inference: ~~(1) the digest,~~ (built, in two slices, 28-29 Aug 2026) (2) the message-latency fix, ~~(3) web push versus a thin native notification shell~~ (weighed 1 Sept 2026, deferred with a trigger; see the push paragraph above).** *(Amended 29 Aug 2026: with the digest built, **the message-latency fix is next**, and the push question is now genuinely answerable rather than premature, because the digest is the notification channel it was always going to be weighed against. Nothing new was added to the order.)* Third place is deliberately a brainstorm rather than a commitment; he expects to weigh it and may not act on it, and it is written down here precisely so neither of us has to carry it in our heads. The queue behind those three still holds the README rewrite and the missing way into a second group. **(Amended 1 Sept 2026: all three are closed and nothing is settled as next. The queue holds message-send latency slice two, the second-group entry point, and site health monitoring; the README rewrite landed 31 Aug.)** **(Amended again 1 Sept 2026, later the same day: latency slice two is closed too, without being built, on a production measurement; §11. The second-group entry point and site health monitoring were both started that day, concurrently, in their own worktrees, which is the first concurrent pair since PR #95 and #96 made worktrees safe for this repo's test runner. The queue behind them holds the unbounded message query, triggered at 200 messages in any group.)** **(Amended 1 Sept 2026, on the site-health-monitoring merge, which landed after the amendment just above and conflicted with it in this very paragraph: site health monitoring is **built and merged**, so of that concurrent pair only the second-group entry point is still open. The queue behind it holds the unbounded message query and the **Supabase auth soft-fail** that slice's whole-branch review surfaced, described in the site-health paragraph above: auth degrading logs every member out silently, and neither the new health check nor the planned log drain can see it. Unranked, the owner's to place.)** **(Amended 1 Sept 2026, the third amendment of this day and deliberately after the two above rather than merged into them: the **second-group entry point is built**, so both halves of that concurrent pair are closed and nothing is settled as next. The queue behind them holds the unbounded message query (triggered at 200 messages in any group) and the Supabase auth soft-fail the site-health review surfaced, both unranked and the owner's to place.)** **(Amended 1 Sept 2026, evening, and this one settles an order rather than reporting a merge. The owner is holding his friend's tennis group, the product's first real users other than himself, until two things land: (1) **"ready for other people's data"**, a privacy notice, terms, a real deletion path, and three verification checks (whether sign-in codes can be guessed repeatedly, whether the unsubscribe endpoint can be abused, and dependency advisories); then (2) **the founder being able to fix a group's details after creation**. The Vercel log drain sits behind both, unranked. **The reasoning behind the order changed mid-conversation, so the stale half is recorded rather than the conclusion alone:** the data slice went first because real users were arriving within days and would meet a product with no privacy notice and no way to delete themselves. The owner then decided to hold the launch until both are done, which removes that deadline entirely. It stays first on the weaker but sufficient ground that it is the smaller and more predictable of the two, leaving the larger one a clean run. **Timing, because "both inside a day" was a hope rather than an estimate:** the first is plausibly a day including its QA; the second is not, since it touches Orbit's decline paths and may need eval bench runs, which cost money and hit the network.)** *Sequencing worth noticing rather than coincidence:* the digest is what finally makes "Email reminders are on." true, the latency fix is the one item the owner has felt on his own phone, and the push question only becomes answerable once the digest has shown what a notification channel is actually worth here.) **(Amended 1 Sept 2026, on the ready-for-other-peoples-data merge, the fourth amendment of that day: the first of the owner's two launch-blocking slices is **built**, so the second, the founder being able to fix a group's details after creation, is next and is the only thing left between the product and the tennis group. The Vercel log drain still sits behind both, unranked, and the queue behind them holds the unbounded message query, the Supabase auth soft-fail, and the two intermittent test flakes registered above.)** **(Amended 2 Sept 2026, later the same day: **cancelling one occurrence is BUILT**; the paragraph above says what it landed as, and build-notes §11 carries the reasoning. Nothing is settled as next. The queue holds propose-a-cancel by group vote, the immediate cancellation email, the founder editing group details (designed and parked, spec at `docs/superpowers/specs/2026-09-02-founder-fixes-group-details-design.md`), the "next week" horizon fix, verbal RSVP, the Vercel log drain, the unbounded message query, and the Supabase auth soft-fail. **The requirement the parked editing slice created still stands and is not optional: with no correction path in the product, the owner sits with the founder and checks the playback screen before the invite link goes out.**)** **(Original amendment follows, kept for the reasoning behind the swap. Amended 2 Sept 2026, and this reorders rather than reports: **the editing slice is designed and parked before any code, and cancelling one occurrence of a recurring event took its place as next.** Spec at `docs/superpowers/specs/2026-09-02-founder-fixes-group-details-design.md`, five product decisions settled inside it. Why the swap, in the owner's words plus the sharpening: the tennis group meets on one fixed day, so changing a group's day is a nice-to-have for them, while cancelling for weather is their confirmed weekly reality; and the editing slice's real day-one risk, a founder unable to correct what Orbit misread at creation, is covered without code by **the owner sitting with the founder and checking the playback screen before the invite link goes out**. That is now a requirement rather than a suggestion, because with editing parked no correction path exists during the trial. Cancel's seven product decisions are settled in build-notes §11 before the build session starts. Behind it: propose-a-cancel by group vote, the immediate cancellation email, the "next week" horizon fix, verbal RSVP, the Vercel log drain, the unbounded message query, and the Supabase auth soft-fail.)** **(Amended 3 Sept 2026, and this one ranks the whole queue rather than reporting a merge. The owner ordered it himself, product-first, after a discussion session that started from a real group-chat message: ~~(1) the "next week" horizon fix,~~ (built 3 Sept 2026, next-week-horizon-fix slice; the paragraph above says what it landed as) (2) the editable event card, (3) group details editing, unparked, (4) verbal group one (cancel, RSVP, gauge votes), (5) verbal group two (venue and day changes), (6) subscribe to calendar, (7) the immediate cancellation email. **Item 1 is done, so the editable event card, item 2, is now the top of this queue.** **Everything from 2 down waits on two weeks of watching the tennis group, and nothing on the list is required before the link goes to them.** Three things this settled that must not be re-derived. **A floated plan can never get a place**: a promoted gauge inherits a venue only when its activity matches a stored rhythm, so tennis gets the court and beers gets nothing, silently, with no edit path anywhere; and venue is not the only thing missing, since a floated plan's title, day and time are equally unfixable. **Group details editing is third on engineering grounds, not product ones**, because it and the editable event card write the same fields onto an existing event, so the card builds that path in the open and the parked slice reuses it; they stay adjacent rather than merged, since merging would couple a ready slice to one carrying nine open decisions. **Its unpark trigger, written as a trigger rather than a position: the day the owner decides to hand the link to a founder he will not be sitting beside**, because that slice is what lets him stop being in the room. Also surfaced and not fixed: a group time-change vote moves one occurrence and never the rhythm, so a group voting Saturday tennis from 9am to 8am gets 9am again the following week with nothing explaining why. Reasoning in build-notes §11, "what Orbit can hear, and what a floated plan can never get".)** **(Amended 4 Sept 2026, on the deploy-reaches-open-tabs merge, and this adds two items rather than reporting a merge. Both came out of that slice's independent review, both are UNRANKED and the owner's to place, and both are recorded here rather than only in build-notes because this paragraph is what a future session reads back when he asks what his queue looks like.** **(1) The poll's failure handling.** A dropped connection during `LiveRefresh`'s poll already takes the same hard-reload path a deploy does: `fetchServerResponse`'s own catch logs "Failed to fetch RSC payload... Falling back to browser navigation." and falls into `doMpaNavigation` -> `location.replace()`, verified in `router-reducer/fetch-server-response.js`. So a member on patchy signal can be hard-reloaded, and a reload fired while the connection is down may land them on a browser error page rather than on a stale-but-working group home. Pre-existing, nothing to do with that slice's own change. **The build recommendation, stated as a recommendation and not a ranking: above everything else currently queued**, on the grounds that it hits ordinary members on ordinary phones every time signal drops, where the deploy case hits rarely and benignly; that reasoning is engineering-shaped about frequency and product-shaped about who it reaches. **(2) The privacy notice's cookie sentence is now untrue.** `src/app/privacy/page.tsx` says the app's cookies "keep you signed in, and that is all they do", and the composer's parked draft means the product now stores **message content** on the member's device. Small, blocks nothing, and it matters because the tennis group is the next thing through the door and that page is a promise made to them. Reasoning for both in build-notes §11, the postscript to "the deploy already reached the tab, and nobody had ever looked".)** **(Amended 4 Sept 2026, hours after the amendment above and correcting it: the deploy-reaches-open-tabs slice's headline claim was FALSE, so the thing it said was already working is now a queued item rather than a shipped one. **Ranked by the owner the same day: build our own deploy detection**, a small endpoint returning the current deployment id plus a plain `fetch()` to compare against, because a `fetch()` we write is not framework-managed and is therefore **not** pinned by Skew Protection, proven in production. That approach works whatever the Vercel setting is, and it hands back control over *when* to reload, which is the product question Skew Protection's own instant reload would answer for us. Two alternatives were weighed and not taken, recorded so they are not re-derived: turning Skew Protection off in the Vercel dashboard (one toggle, no code, but it discards version locking wholesale), and shortening its max age (blunt, and a day is still a day).)** `docs/design/design-polish-rd-2/` remains the build source and rd-1 is lineage only; `finish-layer.css` is declined and never loaded.

**Event titles no longer name a day, and onboarding's two prompts have a bench (20 Aug 2026).** An event's title is the founder's title-cased activity and nothing else, on every path including single-day groups, which is what the spark path already did; the date sits directly under the title on every surface, so a weekday in the title was duplicated information that could only go stale, and a single-day group's title went stale the moment the group moved a plan. The group name stays model-generated, ~~with wording that forbids weekday names, caps it at three words, and bans wordplay~~ (amended 20 Aug 2026, narrow-weekday-rule slice: the wording only bars a weekday name for a group meeting on more than one day, since a group that meets only on Saturday is rightly named after Saturday; the three-word cap and the no-wordplay rule are unchanged); the deterministic fallback is now the bare activity ("Climbing"). It stays model-generated because the founder edits it on the playback card before the group exists, so a bad suggestion costs one edit and never reaches the group. The extraction prompt now asks for the naming form of the activity ("climbing", not "climb"), because the title otherwise varied run to run on identical input. `npm run eval:onboarding` is the new bench: fourteen cases (eleven extraction, three merge) through the real production path, scored per assertion rather than per case, deliberately outside the test runner like the detect bench. ~~Weekday-free group names went from 1/45 to 44/45 and 42/45 over two runs; that residual is accepted rather than rejected in code, on the same reasoning that kept the model.~~ (Amended 20 Aug 2026, narrow-weekday-rule slice: that reading came from six cases that all happened to lead with the activity, and a wider bench showed the miss was not spread evenly at all, it sat entirely on single-day groups, which the owner then ruled were never wrong to name after their day. With the rule narrowed to match, the weekday check now reads clean on every case across two full bench runs. Reasoning and numbers in build-notes §11, "the weekday rule narrows.") Nothing migrated, so groups created before this keep their old titles. **The residual itself closed the same day (multi-day-name-guard fix):** with the rule narrowed, the owner's twice-stated reason for declining a code-side reject no longer applied, so `rejectWeekdayNameOnMultiDay` in `src/lib/orbit/normalize.ts` now catches a weekday-named suggestion (including a plural, "Mondays Climbers") on a multi-day group and swaps it for the derived name before the founder ever sees it; a single-day group's suggestion is untouched. Reasoning in build-notes §11, "a code-side guard closes the residual."

Settled and not open, carried forward: the three-person threshold; below-threshold ideas scrolling away with no residue; **the initiator counted only when they named the day Orbit is proposing** (their message already is that yes, seeded at gauge creation), voting like anyone else when Orbit picked the day; a day counting as named only when exactly one is named; both flavours of no seeding OUT on a created event; a gauge closing two hours before its proposed start, at the start itself for a gauge born inside that window (amended 4 Aug 2026, gauge-endgame slice); zero-yes ideas scrolling away silently while a gauge with at least one yes closes with a single chip-less note (amended 4 Aug 2026, gauge-endgame slice); a threshold reached after the proposed start creating nothing; the retry bar being would-have-cleared and nothing looser, checked before the closure outcomes (4 Aug 2026, wrong-day-retry slice); ask first and guess once, the guess being the same weekday one week after the failed day (4 Aug 2026, wrong-day-retry slice); the loop cap decided by whose move it was, so a member-named revival has full rights and Orbit's own guess never earns a second cycle (4 Aug 2026, wrong-day-retry slice); "answered" meaning a new gauge for the *same* activity opened after the ask, so a pivot to a different activity does not cancel the guess (4 Aug 2026, wrong-day-retry slice); the guess gauge seeding nobody, because Orbit named the day and Orbit is never a vote (4 Aug 2026, wrong-day-retry slice); and time carrying across a retry by copying the original gauge's stored `proposedTime`, never by re-deriving it (4 Aug 2026, wrong-day-retry slice; extended 5 Aug 2026, answer-seam slice: a member's day-answer inherits the same stored time too, a stated time in the answer winning, so "rides the spark path unchanged" no longer holds for time); a third intent class for a reply to Orbit's own open question, live only inside a window deterministic code derives from stored rows and never on the model's word alone (5 Aug 2026, answer-seam slice); the answer reading beating the change reading inside that window, which is a defined precedence and not a widening of the queued both-true discard (5 Aug 2026, answer-seam slice); and an answer naming no single day taking the guess's own same-weekday-next-week date and seeding nobody (5 Aug 2026, answer-seam slice). Reasoning in build-notes §11, spark parts one and two, the wrong-day retry, and the answer seam. From the pending-surface slice: answer-in-place on the surface itself, the same chip and the same server action as chat, rather than a pointer sending the viewer back to scroll (10 Aug 2026, pending-surface slice; one named exception since 12 Aug 2026, the time-change vote, which sits one tap away on the plan's own screen); scope held to the two chip-votable kinds, idea gauges and group time-change proposals, with Orbit's open day-question excluded until it earns a surface of its own, one that suits a question answered in prose rather than by tapping (10 Aug 2026, pending-surface slice; the original wording, ~~until it has a chip-free row type of its own~~, struck 12 Aug 2026, since rows retired with the panel); the tailored-middle view, where unanswered items lead with full chips, ~~a viewer's yeses persist in a quieter group and stay changeable~~ (struck 12 Aug 2026, card-state-grammar slice: the separate "You're in on" grouping retired with the panel, and a viewer's yeses now stay visible and changeable on the card itself), and a decline drops the item from that viewer's view (10 Aug 2026, pending-surface slice); an item leaving the surface the instant it promotes, because the card carousel already carries the viewer's RSVP on the created event (10 Aug 2026, pending-surface slice); no change to how often Orbit speaks in chat (10 Aug 2026, pending-surface slice); ~~the strip absent entirely whenever nothing is pending~~ and ~~the caught-up note firing only when a decline clears the last waiting item, never on a plain yes~~ (both 10 Aug 2026, pending-surface slice; struck 12 Aug 2026, card-state-grammar slice, which retired the strip, the panel, and the caught-up note together). From the day-comment slice: a day named mid-gauge acting as a vote now and as the retry's answer later, never moving the live gauge's own day, chosen over an immediate counter-gauge so one person cannot reset or split a vote others already answered (10 Aug 2026, day-comment slice); can't-that-day as the recorded vote unless the member is already explicitly in, because an inference never overwrites a tap (10 Aug 2026, day-comment slice); exactly one Orbit reply per recorded comment, since Orbit is acting on an inference and the transparency guardrail says so, never a message per later tally change (10 Aug 2026, day-comment slice); a remembered day skipping the retry ask entirely and reviving on that day at close, with the eligibility bar itself unchanged (10 Aug 2026, day-comment slice); a day comment on Orbit's own guess gauge earning that guess a revival, because only a human resets the clock and this is a human move (10 Aug 2026, day-comment slice); the remembered day stored on the gauge row with latest-wins overwrite rather than per vote, ranked or tallied day preferences declined until something needs them (10 Aug 2026, day-comment slice); the day-comment reading outranking the change reading inside the live-gauge situation, a defined precedence like answer-beats-change and not a widening of the queued both-true discard (10 Aug 2026, day-comment slice); a day counting as named only when exactly one is named and it differs from the gauge's own day, so "Saturday works for me" on a Saturday gauge stays the written-but-unbuilt verbal-RSVP guardrail (10 Aug 2026, day-comment slice); and a sweep delayed past the named day falling back to the ask on an ordinary gauge but to the plain close on one of Orbit's own guess gauges (10 Aug 2026, day-comment slice; decided by the build controller in the owner's absence and surfaced in the PR's open questions). From the card-state-grammar slice (all 12 Aug 2026): teal never leaning an open question, so an unanswered RSVP pair carries teal on both borders and only a chosen answer holds the fill, while chips stay grey everywhere; one card list in pure date order with confirmed plans and ideas mingled, a same-instant tie going to the confirmed plan, chosen over confirmed-first because the soonest item is the most actionable thing on the screen precisely when it is still a maybe; a combined cap of five, the owner's answer to cap collisions in place of a slot-guarantee rule, with overflow simply not shown in the region since chat always carried it; the need-label ladder naming a card's highest outstanding need, the viewer's own before anyone else's, teal when the need is theirs and grey when it is other people's, and absent entirely when the card needs nothing; a viewer's decline dropping that idea card for that viewer alone, the vote still recorded and still arriving as an OUT RSVP if the idea promotes; an idea titled as the activity plus a question mark ("Beers?"), chosen over a colder "Pending" prefix; "Place TBD" as fixed copy on an idea card's when-line rather than stored data, so no gauge holds a venue before promotion; ~~an open time-change vote showing on its event's card as a recessed one-line notice and~~ (struck 14 Aug 2026, card-region-height slice: the card's pointer to the vote is gone, the vote itself now raised and tallied in chat instead) living on that event's own screen beside "Add to calendar" (originally just above it; since 17 Aug 2026 just below it, calendar-placement micro-PR), the one named exception to answer-in-place; ask copy staying objective and impersonal and composed from stored facts only, because the product never stores why anyone asked and naming a person's constraint turns "what time works?" into "how do we accommodate Sam?"; and the caught-up note retiring with the panel it lived in, with nothing in its place, a deliberate silence rather than an oversight. Reasoning in build-notes §11, card state grammar.

~~**Queued post-MVP from the 17 Aug phone pass: a word back after a time-change vote.**~~ **Built 18 Aug 2026** in the time-change-ending slice, in the anti-clutter-safe form: a rendered line on the event screen for a viewer who has voted, never a chat message. Original entry kept below for its reasoning. Tapping a chip records the vote and marks it with a checkmark, and says nothing else, so a member who votes into a proposal still two yeses short cannot tell the difference between "counted, not enough yet" and "nothing happened". The owner asked for something short ("thanks for your vote, we'll let you know if the time changes") and queued it post-MVP. Build note recommends it land inside "the time change gets an ending" instead, since that slice opens this surface anyway and the tally that used to carry this signal is gone; the owner's call stands until he changes it.

**Queued from the recognition sweep: the both-true claim.** A message the model reads as both a fresh idea and a request to change an existing plan is discarded whole, so a two-part ask gets nothing back. Its own slice by the owner's call (29 July), because fixing it means deciding which half wins rather than deleting a guard, and the bench cannot see it yet. Open questions in build-notes §11.

**Written as a guardrail, not built: verbal RSVP.** The "act on clear intent" bullet below says "see you Monday" in chat is an RSVP signal to act on. Nothing implements it. The classifier answers four questions about a message (fresh idea, change to a plan, since 5 Aug an answer to Orbit's open day question, and since 10 Aug a better day for an idea being gauged) and none of them is about attendance, so a member saying "yep, works for me" leaves the card reading TBD. Correct for today's build, and a candidate slice; found in the 29 July QA (build-notes §11).

**Queued from the 14 Aug QA (build-notes §11, chat-feed-boundary postscript), none of it pre-MVP except the height work above.** Background colour is no longer the lever for separating an idea card from a confirmed one; the next attempt is structural (a corner mark was floated, undecided), and the card's two existing text signals both failed to land. ~~The time-change tally's wording reads as a symmetric race to three when keeping is actually the default and only the change carries a bar; correct mechanism, wrong words.~~ (Closed 17 Aug 2026, event-copy pass: the line was deleted rather than reworded, on the reading that no brief line can state a compound bar truthfully.) Answering a time-change with checkboxes ("both 7 and 8 work") instead of an either/or is wanted and deferred as too much complexity this close to launch. And one open question, not a decision: two different things both feel "pending" to a member, a new idea and a settled plan somebody is moving, which are distinct in the model and identical in a user's head; cutting time-change-on-confirmed is one possible answer among several, with the recurring rhythm the strongest case for keeping some plans hard to move. (Amended 14 Aug 2026, card-region-height slice: that listed option was taken, but only for the card. An open time change no longer appears on the confirmed card, per this same slice's earlier amendment above. The open question itself is not thereby closed: whether a new idea and a settled plan somebody is moving should both read as "pending" to a member is still undecided, and the recurring-rhythm case for keeping some plans hard to move stands exactly as it did before this slice.)

**Still missing, and known:** A founder who spots a wrong detail after creation has no fix path anywhere (wizard, info page, or Orbit); queued post-MVP as "founder can fix group details after creation," and a step-3 back chevron was considered and declined as its vehicle (11 Aug 2026, build-notes §11 postscript). *(Sharpened 1 Sept 2026, and the gap starts earlier than "after creation" implies. At the playback step the founder can edit the group **name** and the **venue**, and nothing else, so a day or time Orbit read wrongly is sitting visible on that screen with no way to correct it there; the only route is going back a step and re-describing the group in different words. Verified in `Step2Playback.tsx`, whose only inputs are the name row and the venue rows. After creation there is no route at all. This is the slice queued immediately after "ready for other people's data", and until it lands a founder should be told out loud: if the playback shows the wrong day or time, go back and rephrase rather than pushing through.)* A wrong venue guess still has no path to being fixed; Orbit's correction path covers time only, and venue, day, and rhythm changes each get an honest decline rather than an edit, until each gets its own slice. That gap carries more weight since 11 Aug 2026, because the calendar file surfaces the stored street address, so a wrong address can now misdirect a phone rather than sit unread. ~~The multi-card carousel shipped as an interim treatment with no active dot, pending a real design handoff. The group home's designed subline ("8 members · group info & invite link") was never built, and Orbit's avatar is still a letter-"O" placeholder rather than its real face everywhere it appears; both are drawn in the mockups and both belong to the end-of-build visual-polish pass.~~ **All three landed with polish slice one (11-12 Aug 2026).** ~~A session belonging to several groups is sent to its most recent one, a placeholder holding a seat for the multi-group home.~~ (Struck 1 Sept 2026, second-group-entry-point slice: a session belonging to several groups is now shown the list at `/groups` and picks for itself. Only a session belonging to exactly one group is still sent straight in, which is not a guess. The seat is no longer held by something wrong.) ~~**Two of the three model behaviors have no eval bench:** onboarding rhythm extraction (`extract.ts`) and the gap-ask merge (`merge.ts`) have only ungraded hand-run scripts, while intent recognition has the 33-case bench. Queued 10 Aug 2026 with a trigger, not a date: benches for those two are the first task of whichever slice next touches onboarding, and a model version change requires all three benched before it lands.~~ **Closed 20 Aug 2026 (titles-stop-naming-weekdays slice): all three behaviors are benched.** The trigger fired as written and was honoured. `npm run eval:onboarding` grades fourteen cases (eleven extraction, three merge) through the real production path. The model-version commitment stands and is satisfiable for the first time. Reasoning in build-notes §8 and §11.

---

## What we're building (the why)

Interplanetary Groups is a group-first coordination app for casual recurring groups: climbing crews, friend groups, families. The problem: groups choose between low-effort coordination ("show up if you want," and nobody does) and high-effort coordination (polls, forms, group texts that feel like planning a wedding for a casual Sunday). Because someone always has to be "the organizer," most plans never happen and groups slowly stop doing things together.

The solution is an AI coordinator named **Orbit** that carries the organizing burden so no human has to. People express intent in plain language; Orbit checks in, gauges interest, proposes concrete plans, and produces a clean event page. The thesis in one line: **the group is the persistent thing, events are activations of it, and nobody has to be the organizer.**

This is a portfolio MVP meant to demonstrate production-quality AI product work. Code quality and product judgment both matter: write code a reviewing engineer would accept, production-readable, with no quick hacks left unflagged, because this codebase will be read by engineers evaluating product-and-engineering fluency.

---

## Product north stars

- **Group-first.** The group is the persistent entity; events are activations. The invite link joins people to the group, never to an event.
- **The organizer role dissolves after creation.** Anyone can initiate; Orbit carries coordination. No admin console, no role hierarchy beyond minimal founder powers.
- **One conversation surface.** The group feed is the only chat. No private Orbit DMs in MVP. All change requests and Orbit actions are therefore public by design.
- **Anti-clutter is the brand.** The founding complaint was notification noise. Every nudge, message, and notification must justify itself. When in doubt, stay quiet.
- **Complexity must be justified by the problem.** YAGNI applies to product scope, not just code. Two touchstones settle scope arguments: "Perfection is achieved not when there is nothing left to add, but when there is nothing left to take away," and "fall in love with the problem, not the solution." (Rationale and how they apply in build-notes §1.)

---

## Orbit's behavior (guardrails)

Orbit is an agent: tools (what it can do) + context (what it knows) + guardrails (when it acts, asks, or stays quiet). The guardrails:

- **Nudge sparingly.** Ping the group only when it changes an outcome. Reminders point to the card; chip responses update a tally, they never post a message per response.
- **Nudge sparingly, but never leave a direct ask hanging. (Amended 28 July 2026.)** The stay-quiet default governs Orbit speaking up on its own initiative; it does not govern replies. When a member is plainly asking Orbit for something and part of it is ambiguous (which plan, what time), Orbit verifies with a short question instead of staying silent. Silence after Orbit itself just acted is the worst case, not the safe case. Loosened by the owner after change-request QA showed direct requests dying unanswered; anti-clutter still governs unprompted nudges. (build-notes §11, change-request part-two seed.) **Scoped 10 Aug 2026, so this is not read wider than it was meant:** "a direct ask" means a request Orbit can act on. A question outside the product ("what's the weather?", "check my stocks") and a read-only question about the group's own plans ("what's open?") both get SILENCE in the MVP, by the owner's decision, and that silence is the chosen behavior rather than a gap to close. Do not add a class to the recognizer to answer or decline either one without the owner asking for it. (build-notes §8.)
- **Every place Orbit decides to speak or stay quiet is listed in build-notes ("Where Orbit decides to speak or stay quiet").** *(~~Queued 27 Aug 2026 with a trigger rather than a date, the same form as the eval-bench gap that fired as written: the time-change vote-counted line at `ProposalSection.tsx:67` speaks to one viewer and has never had a row on that list. **Adding it is the first task of whichever slice next touches `ProposalSection.tsx` or the time-change flow.** Not fixed in the email sign-in slice because writing another slice's row from outside it is how a reference list starts holding guesses.~~ **Fired and closed 29 Aug 2026, digest slice two, which read time-change votes in order to mail them and judged that a touch: the row is written, and the digest's own twenty rows are on the list beside it.** The second trigger of this form to fire as written, which is now the evidence that trigger-not-a-date works.)* Read it before changing how eager Orbit is; the rule lives in code and in prompt prose in separate files, and the 29 July recognition bug was the two disagreeing. Any slice that adds a new such decision adds its line.
- **Concrete-first, override-friendly.** Propose a specific day (at interest-gauge time) and a specific venue (at lock time), then absorb overrides. Don't poll the group with open options on your own initiative. The exception: if members explicitly ask Orbit to help find a time that works for everyone, Orbit may gather availability, but it still converges to a concrete proposed time rather than handing back a Doodle-style grid for the group to sort out. (How many requests trigger this is a post-launch tuning knob; see build-notes.md §1.)
- **Act on clear intent, ask when ambiguous.** "See you Monday" in chat is an RSVP signal; act on it. If unsure, ask ("sounds like you're in, want me to mark you?").
- **Ask if missing, don't guess.** A gap in the group description (e.g. climb time) triggers a clarifying question.
- **The claim-to-fact boundary is `src/lib/orbit/normalize.ts`.** *(Amended 27 Aug 2026, email sign-in slice: there are now **two**, and this wording was written when there was one. `src/lib/auth/email.ts` does the same job for the service rather than the model, normalizing every raw Supabase reply into a fixed result set before any user-facing branch reads it. It is the stronger of the two and its mechanism is the one to copy: each screen's error map is typed off `Exclude<Result, "ok">`, so a new result variant is a compile error at every call site instead of a member meeting a blank line. `normalize.ts` has no equivalent guarantee. Reasoning in build-notes §11, email sign-in.)* Orbit's structured extraction returns raw model output, which is a claim about the world, not a fact. Every user-facing behavior that keys off an extraction result reads the normalized shape from this module, never the raw response. Worked example, the failure this prevents: extraction once reported a field as missing that the schema did not require, which would have triggered a clarifying question to the founder about something the product never needed to know. (build-notes §11 back-references this as "the CLAUDE.md guardrail," so this pointer must stay.)
- **Stored state is not display; carry it, do not regenerate it.** When Orbit merges a new answer into an existing profile, untouched fields are copied verbatim, never re-derived from the description. Worked example, the failure this prevents: across a gap-merge round the activity label drifted from CLIMBING to CLIMB because the model re-derived it from the description instead of carrying the prior stored value. (build-notes §11, gap-ask.)
- **Orbit is never a User or Membership row.** It speaks in the feed through a `MessageAuthor.ORBIT` enum with a null author, never a person record. This is why Orbit can never appear in a roster, a member list, or an RSVP tally. (build-notes §11, group-home-chat.)
- **Venue never gates anything.** Not the completeness gate, not the gap-ask, not group or event creation. A missing or invalid venue always degrades to nothing; it never blocks a founder or stops a group from scheduling. (build-notes §11, venue-capture.)
- **Transparency on changes.** When Orbit changes group details at someone's request, announce it in the feed with an easy revert. Rhythm-altering changes get gauged with the group first.
- **One bump, then let it die. (Amended 4 Aug 2026, wrong-day-retry slice.)** A stalled-but-viable idea earns at most one resurfacing. No pinning, no banners. One carve-out: a day-blocked idea that would have cleared the bar (in-votes plus can't-that-day votes reaching three) additionally gets one ask at its close and one same-weekday-next-week guess the evening after, then dies; that is its whole allowance. "A gauge whose day has passed is dead" is NOT bent by this: every revival is a genuinely new gauge. (Spec: docs/superpowers/specs/2026-08-04-wrong-day-retry-design.md.)
- **Venue/context suggestions key off the group's actual history,** never generic geography.

---

## Data model (load-bearing rules)

- **Many-to-many user-to-group from day one.** Scope events, RSVPs, and Orbit's context per group. The multi-group home is a fast-follow; the backend must be ready so it's additive, not a retrofit.
- **An RSVP attaches to exactly one event. Attendance never inherits from a parent.** No event nesting in the MVP schema, but never build a shortcut (e.g. a group-level RSVP implying event attendance) that would foreclose adding it later via a nullable parent reference.
- **RSVP is per-person status (in or out), never a stored count.** Counts are derived. "No reply yet" is the absence of an Rsvp row, not a third stored enum value; the RsvpStatus enum is IN / OUT only. Why: derived counts cannot drift out of sync with the rows they describe, and a stored count can. RSVP accuracy is the entire value of this product.
- **Event location is a set of venue options (usually one); RSVPs carry an optional venue.** MVP UI stays single-location, but the model supports the split. A venue also carries an optional short display label that Orbit suggests once at creation time (a Level 1 prompt), stored on the venue and editable, never regenerated per render.
- **Events store a start and an optional end** (multi-day gatherings are date ranges).
- ~~**Emails are never displayed anywhere in the UI,** even after capture.~~ **An email is never shown to the group or to any other member; it is always shown to its owner, on the group info page, and nowhere else.** Member lists are names only. Why, unchanged: the email is given to Orbit, not to the group. A member hands it over to get reminders and to get back in from another device; displaying it to everyone repurposes it into something they never agreed to. A row only its owner can see is not that. *(Amended 27 August 2026, after the owner ran the product on his phone. Task 6 of the email sign-in slice read the old wording literally, so the group info row offered "Change email" with nothing on screen saying which of his addresses he was replacing. The rule had been written around its worst case rather than around its own stated reasoning, which is entirely about the group seeing an address. Scope of the amendment, and it is narrow on purpose: exactly one surface prints an address, exactly for the viewer whose address it is, fetched only for that viewer and never over the roster. Guard: `src/app/__tests__/no-email-address-on-screen.test.tsx`, whose layer 2 is what holds the "never over the roster" half up.)*
- **The server-side completeness gate is the only door to group creation.** No request path creates a group without a schedulable primary rhythm at position zero; the confirm action re-validates the client-held payload on the server before anything is written. (build-notes §11, founder-onboarding.)
- **Deleting a person keeps the conversation and takes their answers (added 1 Sept 2026, ready-for-other-peoples-data slice).** Their messages stay in the feed and stop carrying their name, reading **"Former member"** wherever a name would have gone, on the three fallbacks that used to disagree ("Member", "A former member", "Someone"). Why a label rather than a blank: a nameless bubble reads as a quiet active member, and "former" says what happened to *the group*, which is what a reader needs, where "deleted" says what happened to a database row. Their "Jesse joined" line goes with them, because it exists only to name them. Every answer they gave goes too, RSVPs, gauge votes and time-change votes, on past plans as well as future ones, and the visible cost is accepted rather than unnoticed: a past event that read "4 in" afterwards reads "3 in". A founder cannot be deleted while other people are in their group; the request is refused, naming the groups and their other members, and somebody has to take each group over first. A founder alone in a group takes the group with them. (build-notes §11, ready for other people's data.)
- **Worked example of the "design element with no data home" rule (which lives in the user-level file, not restated here).** A mockup showed a "beers once a month" row alongside the climbing rhythm; no field held a monthly cadence, so a one-shot build shipped everything else and flagged nothing. This is the signature silent-drop failure. The lesson for this codebase: when a screen implies data the model cannot store, name it and settle where it lives before building; never build the parts with a data home and quietly skip the part that needs a modeling decision. (build-notes §11, one-shot experiment.)

---

## Identity, auth, and known gaps

- **Anonymous session on entry,** upgraded later by attaching an email (then ~~magic-link~~ **one-time-code** sign-in). ~~Orbit asks for the email after the user's first RSVP~~ Orbit asks after the member's **first contribution of any kind** (an RSVP, a chat message, or a gauge vote), with a concrete reason attached. Why anonymous-first: experience before PII (build-notes §3). People see and use the product before being asked for anything personal. (Amended 27 Aug 2026, email sign-in slice. Two operative rules that must not drift: **the ask is rendered for one viewer and is never posted to the feed** (a bottom sheet since 27 Aug 2026, an inline note before that; the surface changed, the rule did not), because the feed is public and its only input is the chat composer, so an ask answered there would post a member's address into the group; and **it is a typed code, never a link**, because a link opens in the mail app's own browser, which has different storage from the one the person is sitting in. Reasoning in build-notes §3 and §11.)
- **Invite-link taps must check for an existing session first** and route members in, rather than creating duplicate accounts.
- **Names are the only identity needed to participate** (founder gives theirs in onboarding Step 1; members on the join screen).
- **Deletion is by request, at `privacy@interplanetarygroups.com`, within seven days, and never self-service (added 1 Sept 2026).** The deciding fact is a missing key rather than effort: the product holds no key able to delete a Supabase login, so a button in the app would wipe somebody from our database, leave their login alive, and let the next tap on an invite link recreate them as a brand-new person. The owner runs `npm run person:delete`, which builds and prints a plan and makes him type the person's name before anything is written, then finishes the login by hand in the Supabase dashboard; `docs/runbooks/person-deletion.md` is the procedure and carries the founder-handover case, which is deliberately unscripted. Revisit self-service only if the audience grows past a handful, and it would need the service-role key this product deliberately does not hold (build-notes §8).
- **Membership is a real boundary as of 11 Aug 2026 (share-readiness slice).** The group home, event detail, group info, and the calendar file are members-only: anyone else gets the invite-only note, which names nothing about the group. Every write path refuses a non-member server-side (amended 23 Aug 2026, pre-deploy-fixes slice: through `src/lib/auth/membership.ts`, but not through one uniform call shape. The audit's A-2-6 found `src/app/actions/proposal-vote.ts:82` using the pre-write form, outside the transaction it protects. The wall holds; the "shared check" wording overstated how evenly it is applied). What is still open, and lives at the §8 access-control item, is anything beyond the wall: a request-to-join flow, and any deliberate loosening. ~~The known cost, carried on purpose: a member who loses their session meets the wall until email sign-in exists, and the invite link is their way back.~~ (Struck 23 Aug 2026, pre-deploy-fixes slice: that reading was too kind and it was wrong. Losing a session does not lock a member out, it duplicates them. They still have the link, they tap it, they have no session, so they join as a *second* member: the group holds two of them, their earlier answers belong to an identity nobody can reach, and every count is quietly wrong, in the one product whose whole claim is accurate attendance. Nobody sees an error, and one cache clear or one switch from phone to laptop is enough. This is the strongest argument for moving the email arc forward, and it is why **email sign-in is the slice immediately after the deploy**, by the owner's call, 23 Aug 2026. Reasoning in the audit's 23 Aug postscript.) **Updated 27 Aug 2026 (email sign-in slice), and this is what is now true.** A member who attached an email gets back in as themselves from any device, at `/signin` or from the invite screen, with a typed code. A member who never attached one still duplicates exactly as described above, because nothing about them was ever recorded; the difference is that the invite screen now asks an unrecognised visitor whether they have been here before, so somebody who *has* an address is offered the door instead of walking past it. Attaching is optional forever and Orbit asks twice, so this population never empties. **Duplicates that already exist are not merged**, by decision, and the founder's remove button on the group info page is still the cleanup; the rejoin hole underneath it (audit finding 10) is still open. Signing in does not recover a group only the duplicate ever joined: it drops off the front door with nothing on screen saying so.

---

## UI & copy rules

### Time

- **Everything renders in the group's timezone, never viewer-local.** Forced, not chosen: Orbit's announcement is a single stored string in a shared feed, so a viewer-local card would contradict a group-time announcement for anyone in another zone. Group-time is also how casual groups actually talk ("the 8am climb" is a fact about the group). (build-notes §11, timezone slice.)

### Color

- **Teal marks an action that genuinely matters: a weight, not a count.** More than one may appear when more than one truly matters ("I'm in" and "Add to calendar" are both real). Never on a secondary or incidental action ("Edit my description", back and exit links), which stay outlined or text links; never decorative, never on a chat bubble. Use it sparingly: a screen where most things are teal has said nothing. A card's own action is teal (a teal footer band on the onboarding playback card; the event card's RSVP pair is an ask-and-answer control, both borders teal while unanswered, the chosen answer filled after).
  - **Amended 12 Aug 2026 (card-state-grammar slice): teal never leans an open question.** When a control offers two or more equally valid answers, the options carry equal weight while open: both quiet (the gauge chips) or both carrying the same teal mark (the unanswered RSVP pair's borders, marking the ask itself). Only an answer the member chose may hold the teal fill. A need label naming the viewer's own move (NEEDS YOUR RSVP, NEEDS YOUR VOTE) renders in teal; a label naming other people's move (NEEDS OTHER VOTES) stays grey. Scanning the card region for teal is scanning for what needs you. Scope: chips stay grey everywhere, because chips are the poll on a maybe and the RSVP pair is the ask on a real plan. (build-notes §11, card-state-grammar slice.)
  - ~~**Amended 11 Aug 2026 (strip-placement call)**: the event card no longer holds the group home's only teal; the pending strip's band carries a 7% translucent wash of the action teal (`tint-a`, `rgba(24,188,203,.07)`), an attention weight on a genuine action surface, far quieter than any button and greyscale-safe. (Strip-placement decision record in specs; build-notes §11.)~~ **Retired 12 Aug 2026 with the pending strip itself; the wash left with it (card-state-grammar slice).**
  - **Amended 11 Aug 2026**, from "the single primary action per *element*". The .ics slice's teal "Add to calendar" sat below a teal "I'm in", which the old wording allowed only on the technicality that the two sit in different regions. The owner's read: both actions really are important, and a rule you have to argue around is written wrong. (build-notes §11, ics slice.)
  - **Amended 27 July 2026**, from "exactly one teal action per screen". The multi-card carousel put a teal "I'm in" on each of two upcoming events. The owner's read: each card is its own element with its own primary action, so the original wording was too rigid rather than the code being wrong. The intent it protected is unharmed, because nobody looking at two event cards is confused about which button belongs to which plan. (build-notes §11, spark part two.)
- **Lime is Orbit's brand color, not an action.** Orbit's avatar, brand moments, and gap-prompt cues (the lime "what time?" prompt) are lime. Lime never marks a primary action and never appears as a plain button. Why: on the onboarding cards, lime means "Orbit needs this before it can proceed"; a lime button anywhere else would teach founders that the lime gap marker is sometimes ignorable. (build-notes §11, venue-capture.) (Lime and teal are the two fixed chromatic decisions because each carries meaning; their exact values live in the design tokens.)
- **Dark is the default theme, and as of polish slice one it genuinely is one.** The product is designed dark-first across every screen. ~~Known scaffolding gap: `globals.css` still ships create-next-app defaults, a white `:root` background that only flips dark under `prefers-color-scheme: dark`, and an Arial `body` font while `--font-sans` is wired to Geist.~~ **Both closed 11 Aug 2026 (polish slice one):** `:root` is unconditionally dark and declares `color-scheme: dark`, the `prefers-color-scheme` fork is gone, and the body applies Geist. The palette itself is now the design system's (`--surface-base`, `--surface-low`, `--surface-raised`, `--surface-self`, `--hairline`, `--text-primary`, `--text-secondary`, `--placeholder`, `--text-faint`, `--lime`, `--lime-ink`, `--action`, `--action-ink`), defined in `globals.css` as the single source. The four surfaces are declared in brightness order and mean elevation: base is the page, `--surface-low` an idea card (added 13 Aug 2026, chat-feed-boundary slice), raised a confirmed card or an Orbit bubble, self the viewer's own bubble. Two rules live in that stylesheet rather than inline because a pseudo-element cannot be expressed in a style object: `.scrollbar-hidden` and the `::placeholder` color.
- **Status by brightness plus icon or label, never by hue.** RSVP and event states are distinguished by brightness and an icon or text label (a checkmark for in, grouped IN / OUT / HAVEN'T REPLIED labels), never by color alone.
- **Never rely on red/green as the only signal** (accessibility: the product owner is red/green colorblind). Use shape, label, or position alongside color.

### Type & sizing

- **One locked type scale, applied across all screens.** Sizes are in rem so they honor the device text setting; line-heights are unitless. The `--type-*` size tokens and `--leading-*` line-height tokens are defined in `src/app/globals.css`, which is their single source of truth, kept deliberately separate from the `--text-*` color tokens so a size change never touches color. Do not restate the values here; read them from the CSS so the two copies cannot drift.
- **Role to token mapping** (this is the part the CSS cannot express): group identity name at display; event-detail title at title; preview-card title and home header at heading; body and chat at body; primary and share CTAs at body; compact in-card buttons (RSVP, leave) and the full-width add-to-calendar pill at label (placement settled 11 Aug 2026: event detail only, never in-card); metadata, status, and Orbit's reference note at meta; uppercase eyebrows at the floor. **Nothing anywhere goes below the 13px eyebrow floor.**
- **Layout grows with content, never clips.** Use min-height plus padding, not fixed heights. Containers grow with their text. Buttons stack when they cannot sit side by side. Why: enlarged device text must not clip the event card; proven at enlarged text sizes during the visual-language phase. (build-notes §7.)

### Chat voice system (distinguished by structure and weight, not specific color)

- Orbit speaks as its avatar, no name label, in a soft muted fill. Members speak as a name label, no avatar, in an outlined low-fill bubble. The viewer is right-aligned in the strongest fill, which must not be the teal primary-action color (a teal self-bubble would read as a button) and is not lime (which reads as Orbit).
- **Bubbles for dialogue, notes for reference.** Use a chat bubble only when the user's next on-screen action responds to Orbit. On reference pages, Orbit leaves a labeled note, never a bubble. Why: a bubble promises that the user's next on-screen action answers Orbit. Concrete failure (logged in the feel-pass register): the gap-step card renders as a second Orbit bubble that is a bare data block with nothing to reply to.
- **One onboarding exception, and as of 20 Aug 2026 it is stated as a principle rather than a screen.** A wizard bubble sitting *directly under the header* drops its avatar, shifts to the left margin, and keeps a small tail pointing up at the header, so the header's Orbit mark reads as the speaker. That covers step 1 and step 2's opening bubble; both render through the one shared tailed-bubble component. It does NOT cover the gap-ask, whose bubble sits below the playback card rather than under the header and therefore keeps its avatar like any other Orbit bubble. ~~It is the only tailed bubble in the product.~~ (Amended 20 Aug 2026: the original wording said "the Step 1 bubble" and "the only tailed bubble in the product." The owner's phone QA found two Orbit faces stacked on step 2, which is the exact thing step 1's tail exists to prevent, so the rule was written too narrowly around the screen it was first drawn on rather than around the condition that earns it. Reasoning in build-notes §11, polish slice two.) (Implementation note in build-notes §7.)

### Copy

- **Orbit's voice is plain, warm, and approachable** to everyone from a teen to an 80-year-old (target roughly a 7th to 8th grade reading level). This applies to Orbit's user-facing copy only, not to code, comments, or commit messages.
- **Soft declines everywhere.** "Next time," never "Pass" or a bare "No." Honest tallies depend on socially comfortable exits.
- **Product-voice rule: no em-dashes in anything Orbit says.** This governs Orbit's user-facing copy specifically. Use commas, periods, parentheses. (Standard hyphens in compound words are fine.) It is distinct from the communication rule in the user-level file, which governs what the build agent writes to the product owner. Both exist and both hold; do not collapse them into one or delete either as a duplicate.
- **Three-letter weekday abbreviations** in schedule and rhythm copy ("Mon & Wed mornings @ 8am"), both as a display rule and as something Orbit follows when it generates copy. Why: card real estate; the preview card's job is the gist.
- **Generated display copy is structured-extract-then-format.** Orbit extracts structured fields (days, time, part of day, cadence) and the UI composes the compact display string deterministically; Orbit writes free prose only for its own chat nudges, constrained by format, length, and examples. This is Level 1 AI (prompt engineering) and it also yields the structured data that reminders, the calendar button, and check-ins need. (Why, in build-notes §7.)

### Cards & layout

- **People, not counts, where identity matters.** The event detail roster shows who, by name, grouped IN / OUT / HAVEN'T REPLIED. The compact preview card is counts-only ("4 In · 1 Out · 4 TBD": In always shown, Out only when nonzero, TBD is the pending count, no names), a deliberate brevity choice to keep the card short. (Why, in build-notes §7.)
- **Preview cards show the gist plus the primary action; the detail screen carries completeness.** Counts-only status, the short venue label, and day abbreviations are all instances of this brevity and real-estate discipline.
- **Separator dots** between metadata items are slightly larger and brighter than a hairline so they read as deliberate, still subordinate to the text. Each dot binds to the end of its item, so a wrapped line always starts with a word.
- **Header grammar:** the Orbit logo top-left is the home button (anticipating multi-group); the group title with chevron opens group info. The group emblem lives on the info page and the future multi-group home, not in the header.
- **The event card stays pinned at the top** as the constant next-event reminder; the chat feed is its own scroll region below it, above a pinned input. **The chat body stays at `--type-body` (17px) and is never shrunk to fit.** (An open question about condensing the card after RSVP is recorded in build-notes §7; do not build it preemptively.)

---

## Design sources for this project

The user-level rules say to work from the real source, not a flattened copy. Here is where this project's real source is, and how to treat each artifact.

- **The real visual source is Claude Design's "Send to local coding agent" handoff,** which transfers the actual CSS, the frame components, and the asset files. That handoff is the only thing visual code is built from. Screenshots are never a build input: a screen rendered too small loses fine detail, and any asset the agent never receives is silently replaced with a placeholder that then looks like a deliberate design choice in the diff.
- **`docs/design/walkthrough-screens/` holds cropped PNG exports for the two human verification gates:** the describe-back checkpoint before an agent writes visual code, and human verification of a "matches the design" claim against a rendered screen. They are reference, not source: no CSS, no tokens, no assets. A difference from these screens is a question to raise, not a defect to fix.
- **`docs/walkthrough.html` is a JavaScript-packed export.** Grepping it returns nothing, and a false negative from a grep reads identically to the line genuinely not existing. Any claim about mockup copy requires rendering the file in a browser, never grepping it. This has already produced one wrong claim (build-notes §11, venue-capture).
- **Recorded decisions win over the walkthrough.** Where CLAUDE.md or build-notes disagrees with a walkthrough screen, the recorded decision is correct; the screens predate several shipped decisions.

---

## Stack realities

These are the places where training-data conventions are actively wrong about this repo. Check them before writing code in each area rather than assuming older patterns.

- **Stack:** Next.js 16, Supabase (auth only), Prisma 7, Vitest, Vercel.
- **Next.js 16 changed conventions from earlier versions.** Before writing Next-specific code, read the relevant guide in `node_modules/next/dist/docs/` (flagged in `AGENTS.md`) and heed deprecation notices. If you are not confident about a Next.js 16, Prisma 7, or Supabase detail where conventions have shifted, check the docs before acting instead of guessing; confidence without certainty causes more damage than admitting a gap.
- **Prisma owns the schema.** Prisma 7 keeps its CLI config in `prisma.config.ts`, ships no bundled query engine (a driver adapter is required), and does not auto-run `prisma generate`.
- **Supabase does auth only. The Data API is disabled and Prisma owns the schema.** Reaching for supabase-js to touch data would reintroduce the competing source of truth this project deliberately removed. The single thread between the two systems is the `supabaseAuthId` pointer. *(One declared exception, 27 Aug 2026: `scripts/qa-stage-email.ts` reads `auth.users` read-only through Prisma, so a QA reset can tell the runner what Supabase still holds. It is a hand-run dev-test script and it never enters product code, which is now enforced by a repo-wide raw-SQL and `auth.` schema assertion in `no-email-address-on-screen.test.tsx` carrying an allowlist of exactly that one file. A second such read reddens a test on purpose: that is a decision to bring to the owner, not a test to adjust. Reasoning in build-notes §8.)*
- **A shell-based scan can report clean for a false reason (found 2 Sept 2026).** This project's Next.js route directories carry bracketed names (`join/[inviteToken]/`, `groups/[id]/`, `unsubscribe/[token]/`), which zsh glob-expands, so a plain `grep` over those paths can silently never open the file rather than finding nothing in it. A false-clean scan looks identical to a real one. Route-directory scans in this repo should go through `git diff`, a Python script, or quoted paths, never a bare shell glob.

### Two databases, never crossed

This is the one rule in this file where a mistake is unrecoverable, so it stands on its own. Production and `interplanetary-groups-dev-test` are separate Supabase projects with separate credentials. Never point a production build, migration, or seed script at the dev/test database, and never the reverse. If a task seems to require it, stop and ask.

**How to check which database a checkout points at: `npm run db:which`.** It prints only the Supabase project ref (no URLs, passwords, or keys) and exits nonzero unless all three env sources agree on the dev-test ref, `pxbewardwvoyqqcvogel`. Run it before any migration or seed. Do not read `.env` directly to answer this question; the global sensitive-read guard blocks that on purpose, because the answer sits in the same file as the credentials, and this script is the sanctioned path.

**Before running a production migration, read `docs/runbooks/production-migration.md` first.** ~~It is currently missing two notes (queued, not yet applied there, 31 Aug 2026). The 1Password CLI app integration failed throughout the 31 Aug outage fix and was never resolved on this machine (`op whoami`: "no account found for filter"), so the runbook's `op read` steps may not work as written. The fallback that did work is `read -rs "DIRECT_URL?..."`, which keeps the credential off the screen and out of shell history, the same property the 1Password step exists to provide.~~ **Corrected 2 Sept 2026, and this correction is itself two days late, which is the part worth noticing. The runbook is right as written and needs no notes.** The CLI had only been signed out; `op signin` is the whole fix, and the owner has confirmed that is what he does whenever it stops working. The `read -rs` fallback should never be revived. The retraction was reached on 31 Aug, written as a queued micro-PR, and then lost with the worktree it sat in, so the wrong claim stayed in a file that loads every session for two days and was repeated back to the owner by a session reading it. **A correction parked outside the repository is not a correction; land it or it does not exist.** Also worth knowing and not acting on: the Prisma CLI advertised `8.0.0-rc.12` during that incident; it is a release candidate, unrelated to the outage, and should not be installed.

---

## Out of scope for MVP (don't build, don't design around)

Multi-group home UI · multi-venue UI · event nesting · logistics/travel features · multimodal input (forwarded emails, screenshots) · web push · photo avatars · opt-out attendance preferences. These are fast-follows; the data model accommodates them, the MVP does not implement them. See `docs/build-notes.md` §8.

**Sending a message stopped feeling slow, and the recorded reason for it being slow was wrong (message-send-latency slice one, 31 Aug 2026).** A member's own message reached the screen in milliseconds and then sat drawn at 65% opacity, which reads as *not sent yet*, until Orbit's model call finished, five to six and a half seconds later; the chat input was disabled for the same window, which on iOS also dismisses the keyboard and does not bring it back. Both are gone. **The input is never disabled now**, by the owner's call, so a member can keep typing and send again immediately, and `ChatInput` no longer accepts an `isPending` prop at all so nobody can wire it back to `disabled` without noticing. And a message stops being drawn as sending the moment the server actually has it, rather than when the framework releases its optimistic copy. Measured on a local production build, submit to solid: **6178/6478/5015ms before, 1932/830/1316/1766/1360/1167ms after**, a median of 6178ms down to 1338ms, with the input never disabled and never two copies on screen. What remains of that ~1.3s is the send itself and belongs to slice two.

**The mechanism, because three plausible explanations were wrong before the right one and a future session will otherwise re-derive them.** `GroupHome.tsx` carried a comment claiming Orbit's read "never reaches ChatInput's `disabled`" and calling that "the whole architecture of this slice"; it was false. But the cause was not transition scope either: dispatching Orbit outside every transition changed nothing, dispatching it from an effect changed nothing, and removing its `useTransition` changed nothing in the browser **even though a component test said it worked**. That test mocked the server actions, which strips the very behaviour at issue, and it was deleted rather than kept. **Next dispatches every server action inside a router-level transition, and `useOptimistic` holds its optimistic entry until all of those settle**, so merely calling `detectIntentAction` held the member's message greyed, with or without a transition. Two bigger fixes were offered and declined by the owner on the correct grounds that all three options produce an identical member experience: routing Orbit's read through an API route, and replacing `useOptimistic` with hand-managed state. **Do not re-add a `useTransition` here believing it fixes anything, and do not delete `settledSends` believing the absence of one is what keeps the message solid.** Reasoning in build-notes §11.

**Registered as debt, not fixed:** the framework still holds an invisible optimistic copy of the message for a few seconds after it is drawn as sent (identical content, never seen rendering twice across nine measured sends); and **no test in this repo can hold the timing claim**, because the suite cannot reproduce router-transition behaviour, so the browser measurement is the only evidence and must be re-run by hand. `scripts/measure-group-home.ts` and `scripts/qa-stage-latency.ts` are the instruments, both deliberately outside the test runner.

~~**Slice two is the send itself, and it is now scoped by measurement rather than by the audit.**~~ **(Closed 1 Sept 2026 without being built. On production the owner's three messages took about a second, then under half a second each. The residual this paragraph scopes was mostly the measuring laptop's ~200ms distance from the Ohio database, against 5-15ms on Vercel, so it was largely the bench rather than the product. The figures below stand as measurements and are kept; only the plan to act on them is struck. Also settled that day: prompt caching is structurally unavailable on Haiku 4.5, whose 4,096-token minimum cacheable prefix Orbit's 2,078-token prompt cannot reach, and it would have failed silently; a message costs $0.0047. Build-notes §11, "slice two, closed without building".)** One send costs **seven Supabase identity checks** (confirmed as real network calls: 180-330ms warm, 1250ms cold) and roughly twenty database round trips, and the group home runs eight queries one after another when almost none need each other's answers. The unbounded message query F-6-2 named is real but small (12% of a render at six messages, 25% and 112KB at five hundred), so it is a growth problem rather than today's problem. **Requeued 1 Sept 2026 with a trigger rather than a date: any group crossing 200 messages. A bigger cap is not the fix, because the pain is the rare deliberate scroll-back and a bigger cap only moves the wall; the owner wants a small cap plus a "load older" control.** Two corrections to F-6-2 for whoever picks it up: "re-renders twice per send" is true only when Orbit actually speaks, and every member message costs a full model call even when Orbit then says nothing.

~~**A concurrent worktree breaks this repo's test suite, and the obvious fix is wrong (31 Aug 2026, not fixed).**~~ **Fixed on main the same day by the owner in a separate session, PR #95, which scoped the runner to exclude `.claude/worktrees/**` and separately raised `hookTimeout` to 30s to match `testTimeout`. This branch was cut before that merge and was reading the old config, which is why it hit both and reported them as open; it has since been rebased onto the fix. Entry kept struck rather than deleted because the diagnosis below is still the reference for what this failure looks like, and because the trap in it is still live: the obvious fix is wrong.** Original follows. While another piece of work has a git worktree under `.claude/worktrees/`, vitest collects and runs **its entire suite alongside this one** (2749 tests where there should be 1374), both against the same dev-test database, producing four failures that look like real defects: the email guard's repo-wide walker finding a second `qa-stage-email.ts`, the `run-tests-unless-docs` hook's own selection test, a `supabaseAuthId` unique-constraint collision, and transaction timeouts from pool contention. **Excluding `.claude/**` would also silently disable the four tracked test files in `.claude/hooks/` that guard the safety-net hooks themselves**, caught only because the collected file count dropped 126 to 122; any fix must scope to `.claude/worktrees/**`. Left for the owner, since it belongs to neither piece of work and the email guard reddening is by its own rule a decision to bring to him rather than a test to adjust.
